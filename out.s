    .section .data
    .align 3
L1:
    .asciz "=== SISTEMA DE GESTION DE NOTAS INDIVIDUAL ===\n"

L1_end:

L2:
    .asciz "Procesando 5 estudiantes individuales\n"

L2_end:

L3:
    .asciz "\n--- EVALUACION CON IF/ELSE ---\n"

L3_end:

L4:
    .asciz "\n"

L4_end:

L5:
    .asciz "Clasificacion: SOBRESALIENTE\n"

L5_end:

L6:
    .asciz "Clasificacion: NOTABLE\n"

L6_end:

L7:
    .asciz "Clasificacion: BIEN\n"

L7_end:

L8:
    .asciz "Clasificacion: SUFICIENTE\n"

L8_end:

L9:
    .asciz "Clasificacion: INSUFICIENTE\n"

L9_end:

L10:
    .asciz "Estado: Mayor de edad completa\n"

L10_end:

L11:
    .asciz "Resultado: Apto para beca\n"

L11_end:

L12:
    .asciz "Resultado: No apto para beca\n"

L12_end:

L13:
    .asciz "\n"

L13_end:

L14:
    .asciz "Clasificacion: SOBRESALIENTE\n"

L14_end:

L15:
    .asciz "Clasificacion: NOTABLE\n"

L15_end:

L16:
    .asciz "Clasificacion: REGULAR\n"

L16_end:

L17:
    .asciz "Resultado: Apto para beca\n"

L17_end:

L18:
    .asciz "Resultado: No apto para beca\n"

L18_end:

L19:
    .asciz "\n"

L19_end:

L20:
    .asciz "Clasificacion: NOTABLE\n"

L20_end:

L21:
    .asciz "Clasificacion: BIEN\n"

L21_end:

L22:
    .asciz "Clasificacion: REGULAR\n"

L22_end:

L23:
    .asciz "\n--- PROCESAMIENTO CON WHILE ---\n"

L23_end:

L24:
    .asciz "Buscando estudiantes con nota >= 90:\n"

L24_end:

L25:
    .asciz "\n"

L25_end:

L26:
    .asciz "\n"

L26_end:

L27:
    .asciz "\n"

L27_end:

L28:
    .asciz "\nContador regresivo:\n"

L28_end:

L29:
    .asciz "\n"

L29_end:

L30:
    .asciz "\nBusqueda de estudiante joven con buena nota:\n"

L30_end:

L31:
    .asciz "\n"

L31_end:

L32:
    .asciz "\n--- PROCESAMIENTO CON FOR CLASICO ---\n"

L32_end:

L33:
    .asciz "Listado numerado:\n"

L33_end:

L34:
    .asciz "\n"

L34_end:

L35:
    .asciz "\nProceso inverso:\n"

L35_end:

L36:
    .asciz "\n"

L36_end:

L37:
    .asciz "\nTabla de multiplicar (1-3 x 1-4):\n"

L37_end:

L38:
    .asciz "\n"

L38_end:

L39:
    .asciz "\n--- FOREACH CON ARRAY SIMPLE ---\n"

L39_end:

L40:
    .asciz "Procesando notas con forEach:\n"

L40_end:

L41:
    .asciz "\n"

L41_end:

L42:
    .asciz "\n"

L42_end:

L43:
    .asciz "\nConteo de categorias con forEach:\n"

L43_end:

L44:
    .asciz "\n"

L44_end:

L45:
    .asciz "\n"

L45_end:

L46:
    .asciz "\n"

L46_end:

L47:
    .asciz "\n"

L47_end:

L48:
    .asciz "\n--- CLASIFICACION CON SWITCH/CASE ---\n"

L48_end:

L49:
    .asciz "\n"

L49_end:

L50:
    .asciz "Categoria: EXCELENTE\n"

L50_end:

L51:
    .asciz "Accion: Mantener nivel\n"

L51_end:

L52:
    .asciz "Categoria: MUY BUENO\n"

L52_end:

L53:
    .asciz "Accion: Continuar esfuerzo\n"

L53_end:

L54:
    .asciz "Categoria: BUENO\n"

L54_end:

L55:
    .asciz "Accion: Mejorar tecnicas\n"

L55_end:

L56:
    .asciz "Categoria: SUFICIENTE\n"

L56_end:

L57:
    .asciz "Accion: Reforzar conocimientos\n"

L57_end:

L58:
    .asciz "Categoria: INSUFICIENTE\n"

L58_end:

L59:
    .asciz "Accion: Apoyo urgente\n"

L59_end:

L60:
    .asciz "\n"

L60_end:

L61:
    .asciz "Categoria: EXCELENTE\n"

L61_end:

L62:
    .asciz "Categoria: MUY BUENO\n"

L62_end:

L63:
    .asciz "Categoria: BUENO\n"

L63_end:

L64:
    .asciz "Categoria: REGULAR\n"

L64_end:

L65:
    .asciz "\nAnalisis por edad:\n"

L65_end:

L66:
    .asciz "\n"

L66_end:

L67:
    .asciz "\n"

L67_end:

L68:
    .asciz "\n"

L68_end:

L69:
    .asciz "\n"

L69_end:

L70:
    .asciz "\n--- DEMOSTRACION DE BREAK ---\n"

L70_end:

L71:
    .asciz "Busqueda con break (primera nota > 90):\n"

L71_end:

L72:
    .asciz "\n"

L72_end:

L73:
    .asciz "\n"

L73_end:

L74:
    .asciz "\nBusqueda limitada con while y break:\n"

L74_end:

L75:
    .asciz "Limite alcanzado, usando break para salir\n"

L75_end:

L76:
    .asciz "\n"

L76_end:

L77:
    .asciz "\nBreak en for anidado:\n"

L77_end:

L78:
    .asciz "\n"

L78_end:

L79:
    .asciz "  Break en columna 3\n"

L79_end:

L80:
    .asciz "\n"

L80_end:

L81:
    .asciz "\n--- DEMOSTRACION DE CONTINUE ---\n"

L81_end:

L82:
    .asciz "Procesando solo notas pares (con continue):\n"

L82_end:

L83:
    .asciz "\n"

L83_end:

L84:
    .asciz "\n"

L84_end:

L85:
    .asciz "\nConteo con continue (saltando multiplos de 3):\n"

L85_end:

L86:
    .asciz "\n"

L86_end:

L87:
    .asciz "\n"

L87_end:

L88:
    .asciz "\nMatriz con continue anidado:\n"

L88_end:

L89:
    .asciz "\n"

L89_end:

L90:
    .asciz "\nProcesamiento selectivo con continue:\n"

L90_end:

L91:
    .asciz "\n"

L91_end:

L92:
    .asciz "\n"

L92_end:

L93:
    .asciz "\n--- RESUMEN FINAL ---\n"

L93_end:

L94:
    .asciz "\n"

L94_end:

L95:
    .asciz "\n"

L95_end:

L96:
    .asciz "\n=== PROCESAMIENTO COMPLETADO ===\n"

L96_end:

STRLIT_1:
    .asciz "=== SISTEMA DE GESTION DE NOTAS INDIVIDUAL ==="

STRLIT_2:
    .asciz "Ana"

STRLIT_3:
    .asciz "Luis"

STRLIT_4:
    .asciz "Maria"

STRLIT_5:
    .asciz "Carlos"

STRLIT_6:
    .asciz "Sofia"

STRLIT_7:
    .asciz "Procesando 5 estudiantes individuales"

STRLIT_8:
    .asciz "\n--- EVALUACION CON IF/ELSE ---"

STRLIT_9:
    .asciz "Evaluando: "

STRLIT_10:
    .asciz "Clasificacion: SOBRESALIENTE"

STRLIT_11:
    .asciz "Clasificacion: NOTABLE"

STRLIT_12:
    .asciz "Clasificacion: BIEN"

STRLIT_13:
    .asciz "Clasificacion: SUFICIENTE"

STRLIT_14:
    .asciz "Clasificacion: INSUFICIENTE"

STRLIT_15:
    .asciz "Estado: Mayor de edad completa"

STRLIT_16:
    .asciz "Resultado: Apto para beca"

STRLIT_17:
    .asciz "Resultado: No apto para beca"

STRLIT_18:
    .asciz "\nEvaluando: "

STRLIT_19:
    .asciz "Clasificacion: REGULAR"

STRLIT_20:
    .asciz "\n--- PROCESAMIENTO CON WHILE ---"

STRLIT_21:
    .asciz "Buscando estudiantes con nota >= 90:"

STRLIT_22:
    .asciz ""

STRLIT_23:
    .asciz "Revisando estudiante "

STRLIT_24:
    .asciz ": "

STRLIT_25:
    .asciz "Destacado encontrado: "

STRLIT_26:
    .asciz " con "

STRLIT_27:
    .asciz "Total de estudiantes destacados: "

STRLIT_28:
    .asciz "\nContador regresivo:"

STRLIT_29:
    .asciz "Quedan: "

STRLIT_30:
    .asciz " estudiantes por procesar"

STRLIT_31:
    .asciz "\nBusqueda de estudiante joven con buena nota:"

STRLIT_32:
    .asciz "Estudiante joven destacado: "

STRLIT_33:
    .asciz " (Edad: "

STRLIT_34:
    .asciz ", Nota: "

STRLIT_35:
    .asciz ")"

STRLIT_36:
    .asciz "\n--- PROCESAMIENTO CON FOR CLASICO ---"

STRLIT_37:
    .asciz "Listado numerado:"

STRLIT_38:
    .asciz ". "

STRLIT_39:
    .asciz " - Nota: "

STRLIT_40:
    .asciz "\nProceso inverso:"

STRLIT_41:
    .asciz "Procesando posicion: "

STRLIT_42:
    .asciz "\nTabla de multiplicar (1-3 x 1-4):"

STRLIT_43:
    .asciz " x "

STRLIT_44:
    .asciz " = "

STRLIT_45:
    .asciz "\n--- FOREACH CON ARRAY SIMPLE ---"

STRLIT_46:
    .asciz "Procesando notas con forEach:"

STRLIT_47:
    .asciz "Nota "

STRLIT_48:
    .asciz "Promedio calculado con forEach: "

STRLIT_49:
    .asciz "\nConteo de categorias con forEach:"

STRLIT_50:
    .asciz "Nota alta encontrada: "

STRLIT_51:
    .asciz "Nota baja encontrada: "

STRLIT_52:
    .asciz "Total notas altas (>=90): "

STRLIT_53:
    .asciz "Total notas bajas (<80): "

STRLIT_54:
    .asciz "\n--- CLASIFICACION CON SWITCH/CASE ---"

STRLIT_55:
    .asciz "Analizando: "

STRLIT_56:
    .asciz " (Nota: "

STRLIT_57:
    .asciz "Categoria: EXCELENTE"

STRLIT_58:
    .asciz "Accion: Mantener nivel"

STRLIT_59:
    .asciz "Categoria: MUY BUENO"

STRLIT_60:
    .asciz "Accion: Continuar esfuerzo"

STRLIT_61:
    .asciz "Categoria: BUENO"

STRLIT_62:
    .asciz "Accion: Mejorar tecnicas"

STRLIT_63:
    .asciz "Categoria: SUFICIENTE"

STRLIT_64:
    .asciz "Accion: Reforzar conocimientos"

STRLIT_65:
    .asciz "Categoria: INSUFICIENTE"

STRLIT_66:
    .asciz "Accion: Apoyo urgente"

STRLIT_67:
    .asciz "\nAnalizando: "

STRLIT_68:
    .asciz "Categoria: REGULAR"

STRLIT_69:
    .asciz "\nAnalisis por edad:"

STRLIT_70:
    .asciz " - Categoria: Muy joven"

STRLIT_71:
    .asciz " - Categoria: Joven adulto"

STRLIT_72:
    .asciz " - Categoria: Adulto joven"

STRLIT_73:
    .asciz " - Categoria: Adulto"

STRLIT_74:
    .asciz "\n--- DEMOSTRACION DE BREAK ---"

STRLIT_75:
    .asciz "Busqueda con break (primera nota > 90):"

STRLIT_76:
    .asciz "Verificando: "

STRLIT_77:
    .asciz " - "

STRLIT_78:
    .asciz "Encontrado! "

STRLIT_79:
    .asciz " supera 90 puntos"

STRLIT_80:
    .asciz "\nBusqueda limitada con while y break:"

STRLIT_81:
    .asciz "Limite alcanzado, usando break para salir"

STRLIT_82:
    .asciz "\nBreak en for anidado:"

STRLIT_83:
    .asciz "Fila "

STRLIT_84:
    .asciz ":"

STRLIT_85:
    .asciz "  Break en columna 3"

STRLIT_86:
    .asciz "  Columna "

STRLIT_87:
    .asciz "\n--- DEMOSTRACION DE CONTINUE ---"

STRLIT_88:
    .asciz "Procesando solo notas pares (con continue):"

STRLIT_89:
    .asciz "Saltando "

STRLIT_90:
    .asciz " (nota impar: "

STRLIT_91:
    .asciz "Procesando "

STRLIT_92:
    .asciz " (nota par: "

STRLIT_93:
    .asciz "\nConteo con continue (saltando multiplos de 3):"

STRLIT_94:
    .asciz "Saltando multiplo de 3: "

STRLIT_95:
    .asciz "Numero procesado: "

STRLIT_96:
    .asciz "\nMatriz con continue anidado:"

STRLIT_97:
    .asciz "Celda ["

STRLIT_98:
    .asciz ","

STRLIT_99:
    .asciz "]"

STRLIT_100:
    .asciz "\nProcesamiento selectivo con continue:"

STRLIT_101:
    .asciz " (no cumple criterios)"

STRLIT_102:
    .asciz " (cumple criterios: nota="

STRLIT_103:
    .asciz ", edad="

STRLIT_104:
    .asciz "\n--- RESUMEN FINAL ---"

STRLIT_105:
    .asciz "Mejor estudiante: "

STRLIT_106:
    .asciz "Promedio general: "

STRLIT_107:
    .asciz "\n=== PROCESAMIENTO COMPLETADO ==="

GV_estudiante1: .quad STRLIT_2

GV_estudiante2: .quad STRLIT_3

GV_estudiante3: .quad STRLIT_4

GV_estudiante4: .quad STRLIT_5

GV_estudiante5: .quad STRLIT_6

GV_nota1: .quad 85

GV_nota2: .quad 92

GV_nota3: .quad 78

GV_nota4: .quad 95

GV_nota5: .quad 88

GV_edad1: .quad 20

GV_edad2: .quad 19

GV_edad3: .quad 21

GV_edad4: .quad 20

GV_edad5: .quad 22

GV_estudianteActual: .quad 1

GV_estudiantesDestacados: .quad 0

GV_nombreActual: .quad STRLIT_22

GV_notaActual: .quad 0

GV_contador: .quad 5

GV_indice: .quad 1

GV_nombre: .quad STRLIT_22

GV_nota: .quad 0

GV_edad: .quad 0

GV_i: .quad 1

GV_j: .quad 1

GV_resultado: .quad 0

GV_notasParaForeach: .quad 0

GV_suma: .quad 0

GV_contadorNotas: .quad 0

GV___idx_fe_nota: .quad 0

    .align 8
GV_promedio: .double 0.0

GV_notasAltas: .quad 0

GV_notasBajas: .quad 0

GV_categoria1: .quad 0

GV_categoria2: .quad 0

GV_posicion: .quad 0

GV_numero: .quad 0

GV_fila: .quad 1

GV_columna: .quad 1

GV_pos: .quad 1

GV_notaPos: .quad 0

GV_edadPos: .quad 0

GV_nombrePos: .quad STRLIT_22

GV_notaMaxima: .quad 0

GV_estudianteMejor: .quad 0

GV_sumaTotal: .quad 0

    .align 8
GV_promedioFinal: .double 0.0

GV_estudiante1_str:
    .asciz "Ana"

GV_estudiante1_str_end:

GV_estudiante2_str:
    .asciz "Luis"

GV_estudiante2_str_end:

GV_estudiante3_str:
    .asciz "Maria"

GV_estudiante3_str_end:

GV_estudiante4_str:
    .asciz "Carlos"

GV_estudiante4_str_end:

GV_estudiante5_str:
    .asciz "Sofia"

GV_estudiante5_str_end:

GV_nota1_str:
    .asciz "85"

GV_nota1_str_end:

GV_nota2_str:
    .asciz "92"

GV_nota2_str_end:

GV_nota3_str:
    .asciz "78"

GV_nota3_str_end:

GV_nota4_str:
    .asciz "95"

GV_nota4_str_end:

GV_nota5_str:
    .asciz "88"

GV_nota5_str_end:

GV_edad1_str:
    .asciz "20"

GV_edad1_str_end:

GV_edad2_str:
    .asciz "19"

GV_edad2_str_end:

GV_edad3_str:
    .asciz "21"

GV_edad3_str_end:

GV_edad4_str:
    .asciz "20"

GV_edad4_str_end:

GV_edad5_str:
    .asciz "22"

GV_edad5_str_end:

GV_estudianteActual_str:
    .asciz "1"

GV_estudianteActual_str_end:

GV_estudiantesDestacados_str:
    .asciz "0"

GV_estudiantesDestacados_str_end:

GV_nombreActual_str:
    .asciz ""

GV_nombreActual_str_end:

GV_notaActual_str:
    .asciz "0"

GV_notaActual_str_end:

GV_contador_str:
    .asciz "5"

GV_contador_str_end:

GV_indice_str:
    .asciz "1"

GV_indice_str_end:

GV_nombre_str:
    .asciz ""

GV_nombre_str_end:

GV_nota_str:
    .asciz "0"

GV_nota_str_end:

GV_edad_str:
    .asciz "0"

GV_edad_str_end:

GV_i_str:
    .asciz "1"

GV_i_str_end:

GV_j_str:
    .asciz "1"

GV_j_str_end:

GV_suma_str:
    .asciz "0"

GV_suma_str_end:

GV_contadorNotas_str:
    .asciz "0"

GV_contadorNotas_str_end:

GV___idx_fe_nota_str:
    .asciz "0"

GV___idx_fe_nota_str_end:

GV_notasAltas_str:
    .asciz "0"

GV_notasAltas_str_end:

GV_numero_str:
    .asciz "0"

GV_numero_str_end:

GV_fila_str:
    .asciz "1"

GV_fila_str_end:

GV_columna_str:
    .asciz "1"

GV_columna_str_end:

GV_pos_str:
    .asciz "1"

GV_pos_str_end:

GV_notaPos_str:
    .asciz "0"

GV_notaPos_str_end:

GV_edadPos_str:
    .asciz "0"

GV_edadPos_str_end:

GV_nombrePos_str:
    .asciz ""

GV_nombrePos_str_end:

    .section .text
    .global _start
_start:
    // Inicializar pila para operaciones complejas
    mov x0, sp
    sub sp, sp, #1024
    // ===== System.out.println =====
# interpret_map:
#   PRIMITIVO=0x619b37b153c5
#   IDENTIFICADOR=0x619b37b152c8
#   EXPRESION_LENGUAJE=0x619b37b14c32
#   LLAMADA_FUNCION=0x619b37b1d69a
#   LISTA_EXPRESIONES=0x619b37b0ebfe
#   CAST=0x619b37b145dd
#   UNARIO_LENGUAJE=0x619b37b15092
#   BUILTIN_STRING_VALUEOF=0x619b37b10263
#   PRINT_EXPR=0x619b37b1ce23
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor==== SISTEMA DE GESTION DE NOTAS INDIVIDUAL ===
    // Imprimir string literal '=== SISTEMA DE GESTION DE NOTAS INDIVIDUAL ==='
    adrp x0, STRLIT_1
    add x0, x0, :lo12:STRLIT_1
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con primitivo: estudiante1 = Ana
    adrp x1, GV_estudiante1
    add x1, x1, :lo12:GV_estudiante1
    adrp x2, STRLIT_2
    add x2, x2, :lo12:STRLIT_2
    str x2, [x1]

    // Declaración con primitivo: estudiante2 = Luis
    adrp x1, GV_estudiante2
    add x1, x1, :lo12:GV_estudiante2
    adrp x2, STRLIT_3
    add x2, x2, :lo12:STRLIT_3
    str x2, [x1]

    // Declaración con primitivo: estudiante3 = Maria
    adrp x1, GV_estudiante3
    add x1, x1, :lo12:GV_estudiante3
    adrp x2, STRLIT_4
    add x2, x2, :lo12:STRLIT_4
    str x2, [x1]

    // Declaración con primitivo: estudiante4 = Carlos
    adrp x1, GV_estudiante4
    add x1, x1, :lo12:GV_estudiante4
    adrp x2, STRLIT_5
    add x2, x2, :lo12:STRLIT_5
    str x2, [x1]

    // Declaración con primitivo: estudiante5 = Sofia
    adrp x1, GV_estudiante5
    add x1, x1, :lo12:GV_estudiante5
    adrp x2, STRLIT_6
    add x2, x2, :lo12:STRLIT_6
    str x2, [x1]

    // Declaración con primitivo: nota1 = 85
    adrp x1, GV_nota1
    add x1, x1, :lo12:GV_nota1
    mov x2, #85
    str x2, [x1]

    // Declaración con primitivo: nota2 = 92
    adrp x1, GV_nota2
    add x1, x1, :lo12:GV_nota2
    mov x2, #92
    str x2, [x1]

    // Declaración con primitivo: nota3 = 78
    adrp x1, GV_nota3
    add x1, x1, :lo12:GV_nota3
    mov x2, #78
    str x2, [x1]

    // Declaración con primitivo: nota4 = 95
    adrp x1, GV_nota4
    add x1, x1, :lo12:GV_nota4
    mov x2, #95
    str x2, [x1]

    // Declaración con primitivo: nota5 = 88
    adrp x1, GV_nota5
    add x1, x1, :lo12:GV_nota5
    mov x2, #88
    str x2, [x1]

    // Declaración con primitivo: edad1 = 20
    adrp x1, GV_edad1
    add x1, x1, :lo12:GV_edad1
    mov x2, #20
    str x2, [x1]

    // Declaración con primitivo: edad2 = 19
    adrp x1, GV_edad2
    add x1, x1, :lo12:GV_edad2
    mov x2, #19
    str x2, [x1]

    // Declaración con primitivo: edad3 = 21
    adrp x1, GV_edad3
    add x1, x1, :lo12:GV_edad3
    mov x2, #21
    str x2, [x1]

    // Declaración con primitivo: edad4 = 20
    adrp x1, GV_edad4
    add x1, x1, :lo12:GV_edad4
    mov x2, #20
    str x2, [x1]

    // Declaración con primitivo: edad5 = 22
    adrp x1, GV_edad5
    add x1, x1, :lo12:GV_edad5
    mov x2, #22
    str x2, [x1]

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Procesando 5 estudiantes individuales
    // Imprimir string literal 'Procesando 5 estudiantes individuales'
    adrp x0, STRLIT_7
    add x0, x0, :lo12:STRLIT_7
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\n--- EVALUACION CON IF/ELSE ---
    // Imprimir string literal '\n--- EVALUACION CON IF/ELSE ---'
    adrp x0, STRLIT_8
    add x0, x0, :lo12:STRLIT_8
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=1870438240
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Evaluando: 
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante1
    // Imprimir string literal 'Evaluando: '
    adrp x0, STRLIT_9
    add x0, x0, :lo12:STRLIT_9
    mov x1, #0
    bl print_string

    // Imprimir variable global 'estudiante1' (única: 'estudiante1')
    adrp x0, GV_estudiante1
    add x0, x0, :lo12:GV_estudiante1
    // tipo tag=9
    mov x1, #9
    mov x2, #1
    bl print_any_gv

    // ===== Fin System.out.println =====

    // IF statement
    // Cargar entero literal 95 en registro x8
    mov x8, #95
    str x8, [sp, #-16]!
    // Cargar variable global 'nota1' (única: 'nota1') en registro x9
    adrp x9, GV_nota1
    add x9, x9, :lo12:GV_nota1
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_0
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: SOBRESALIENTE
    // Imprimir string literal 'Clasificacion: SOBRESALIENTE'
    adrp x0, STRLIT_10
    add x0, x0, :lo12:STRLIT_10
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_1
else_0:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 85 en registro x8
    mov x8, #85
    str x8, [sp, #-16]!
    // Cargar variable global 'nota1' (única: 'nota1') en registro x9
    adrp x9, GV_nota1
    add x9, x9, :lo12:GV_nota1
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_2
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: NOTABLE
    // Imprimir string literal 'Clasificacion: NOTABLE'
    adrp x0, STRLIT_11
    add x0, x0, :lo12:STRLIT_11
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_3
else_2:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 75 en registro x8
    mov x8, #75
    str x8, [sp, #-16]!
    // Cargar variable global 'nota1' (única: 'nota1') en registro x9
    adrp x9, GV_nota1
    add x9, x9, :lo12:GV_nota1
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_4
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: BIEN
    // Imprimir string literal 'Clasificacion: BIEN'
    adrp x0, STRLIT_12
    add x0, x0, :lo12:STRLIT_12
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_5
else_4:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 65 en registro x8
    mov x8, #65
    str x8, [sp, #-16]!
    // Cargar variable global 'nota1' (única: 'nota1') en registro x9
    adrp x9, GV_nota1
    add x9, x9, :lo12:GV_nota1
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_6
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: SUFICIENTE
    // Imprimir string literal 'Clasificacion: SUFICIENTE'
    adrp x0, STRLIT_13
    add x0, x0, :lo12:STRLIT_13
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_7
else_6:
    // Bloque ELSE
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: INSUFICIENTE
    // Imprimir string literal 'Clasificacion: INSUFICIENTE'
    adrp x0, STRLIT_14
    add x0, x0, :lo12:STRLIT_14
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

endif_7:
    // Fin IF
endif_5:
    // Fin IF
endif_3:
    // Fin IF
endif_1:
    // Fin IF
    // IF statement
    // Cargar entero literal 21 en registro x8
    mov x8, #21
    str x8, [sp, #-16]!
    // Cargar variable global 'edad1' (única: 'edad1') en registro x9
    adrp x9, GV_edad1
    add x9, x9, :lo12:GV_edad1
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq endif_9
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Estado: Mayor de edad completa
    // Imprimir string literal 'Estado: Mayor de edad completa'
    adrp x0, STRLIT_15
    add x0, x0, :lo12:STRLIT_15
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

endif_9:
    // Fin IF
    // IF statement
    // Cargar entero literal 90 en registro x8
    mov x8, #90
    str x8, [sp, #-16]!
    // Cargar variable global 'nota1' (única: 'nota1') en registro x9
    adrp x9, GV_nota1
    add x9, x9, :lo12:GV_nota1
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_10
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Resultado: Apto para beca
    // Imprimir string literal 'Resultado: Apto para beca'
    adrp x0, STRLIT_16
    add x0, x0, :lo12:STRLIT_16
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_11
else_10:
    // Bloque ELSE
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Resultado: No apto para beca
    // Imprimir string literal 'Resultado: No apto para beca'
    adrp x0, STRLIT_17
    add x0, x0, :lo12:STRLIT_17
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

endif_11:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=\nEvaluando: 
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante2
    // Imprimir string literal '\nEvaluando: '
    adrp x0, STRLIT_18
    add x0, x0, :lo12:STRLIT_18
    mov x1, #0
    bl print_string

    // Imprimir variable global 'estudiante2' (única: 'estudiante2')
    adrp x0, GV_estudiante2
    add x0, x0, :lo12:GV_estudiante2
    // tipo tag=9
    mov x1, #9
    mov x2, #1
    bl print_any_gv

    // ===== Fin System.out.println =====

    // IF statement
    // Cargar entero literal 95 en registro x8
    mov x8, #95
    str x8, [sp, #-16]!
    // Cargar variable global 'nota2' (única: 'nota2') en registro x9
    adrp x9, GV_nota2
    add x9, x9, :lo12:GV_nota2
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_12
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: SOBRESALIENTE
    // Imprimir string literal 'Clasificacion: SOBRESALIENTE'
    adrp x0, STRLIT_10
    add x0, x0, :lo12:STRLIT_10
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_13
else_12:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 85 en registro x8
    mov x8, #85
    str x8, [sp, #-16]!
    // Cargar variable global 'nota2' (única: 'nota2') en registro x9
    adrp x9, GV_nota2
    add x9, x9, :lo12:GV_nota2
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_14
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: NOTABLE
    // Imprimir string literal 'Clasificacion: NOTABLE'
    adrp x0, STRLIT_11
    add x0, x0, :lo12:STRLIT_11
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_15
else_14:
    // Bloque ELSE
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: REGULAR
    // Imprimir string literal 'Clasificacion: REGULAR'
    adrp x0, STRLIT_19
    add x0, x0, :lo12:STRLIT_19
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

endif_15:
    // Fin IF
endif_13:
    // Fin IF
    // IF statement
    // Cargar entero literal 90 en registro x8
    mov x8, #90
    str x8, [sp, #-16]!
    // Cargar variable global 'nota2' (única: 'nota2') en registro x9
    adrp x9, GV_nota2
    add x9, x9, :lo12:GV_nota2
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_16
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Resultado: Apto para beca
    // Imprimir string literal 'Resultado: Apto para beca'
    adrp x0, STRLIT_16
    add x0, x0, :lo12:STRLIT_16
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_17
else_16:
    // Bloque ELSE
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Resultado: No apto para beca
    // Imprimir string literal 'Resultado: No apto para beca'
    adrp x0, STRLIT_17
    add x0, x0, :lo12:STRLIT_17
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

endif_17:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=\nEvaluando: 
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante3
    // Imprimir string literal '\nEvaluando: '
    adrp x0, STRLIT_18
    add x0, x0, :lo12:STRLIT_18
    mov x1, #0
    bl print_string

    // Imprimir variable global 'estudiante3' (única: 'estudiante3')
    adrp x0, GV_estudiante3
    add x0, x0, :lo12:GV_estudiante3
    // tipo tag=9
    mov x1, #9
    mov x2, #1
    bl print_any_gv

    // ===== Fin System.out.println =====

    // IF statement
    // Cargar entero literal 85 en registro x8
    mov x8, #85
    str x8, [sp, #-16]!
    // Cargar variable global 'nota3' (única: 'nota3') en registro x9
    adrp x9, GV_nota3
    add x9, x9, :lo12:GV_nota3
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_18
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: NOTABLE
    // Imprimir string literal 'Clasificacion: NOTABLE'
    adrp x0, STRLIT_11
    add x0, x0, :lo12:STRLIT_11
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_19
else_18:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 75 en registro x8
    mov x8, #75
    str x8, [sp, #-16]!
    // Cargar variable global 'nota3' (única: 'nota3') en registro x9
    adrp x9, GV_nota3
    add x9, x9, :lo12:GV_nota3
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_20
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: BIEN
    // Imprimir string literal 'Clasificacion: BIEN'
    adrp x0, STRLIT_12
    add x0, x0, :lo12:STRLIT_12
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b endif_21
else_20:
    // Bloque ELSE
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Clasificacion: REGULAR
    // Imprimir string literal 'Clasificacion: REGULAR'
    adrp x0, STRLIT_19
    add x0, x0, :lo12:STRLIT_19
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

endif_21:
    // Fin IF
endif_19:
    // Fin IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\n--- PROCESAMIENTO CON WHILE ---
    // Imprimir string literal '\n--- PROCESAMIENTO CON WHILE ---'
    adrp x0, STRLIT_20
    add x0, x0, :lo12:STRLIT_20
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con primitivo: estudianteActual = 1
    adrp x1, GV_estudianteActual
    add x1, x1, :lo12:GV_estudianteActual
    mov x2, #1
    str x2, [x1]

    // Declaración con primitivo: estudiantesDestacados = 0
    adrp x1, GV_estudiantesDestacados
    add x1, x1, :lo12:GV_estudiantesDestacados
    mov x2, #0
    str x2, [x1]

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Buscando estudiantes con nota >= 90:
    // Imprimir string literal 'Buscando estudiantes con nota >= 90:'
    adrp x0, STRLIT_21
    add x0, x0, :lo12:STRLIT_21
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // WHILE statement
while_loop_22:
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'estudianteActual' (única: 'estudianteActual') en registro x9
    adrp x9, GV_estudianteActual
    add x9, x9, :lo12:GV_estudianteActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq while_end_23
    // Bloque WHILE
    // Declaración con primitivo: nombreActual = 
    adrp x1, GV_nombreActual
    add x1, x1, :lo12:GV_nombreActual
    adrp x2, STRLIT_22
    add x2, x2, :lo12:STRLIT_22
    str x2, [x1]

    // Declaración con primitivo: notaActual = 0
    adrp x1, GV_notaActual
    add x1, x1, :lo12:GV_notaActual
    mov x2, #0
    str x2, [x1]

    // IF statement
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'estudianteActual' (única: 'estudianteActual') en registro x9
    adrp x9, GV_estudianteActual
    add x9, x9, :lo12:GV_estudianteActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_24
    // Bloque IF
    // Asignar identificador 'estudiante1' (único: 'estudiante1') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante1
    add x1, x1, :lo12:GV_estudiante1
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    // Asignar identificador 'nota1' (único: 'nota1') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota1
    add x1, x1, :lo12:GV_nota1
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    b endif_25
else_24:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 2 en registro x8
    mov x8, #2
    str x8, [sp, #-16]!
    // Cargar variable global 'estudianteActual' (única: 'estudianteActual') en registro x9
    adrp x9, GV_estudianteActual
    add x9, x9, :lo12:GV_estudianteActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_26
    // Bloque IF
    // Asignar identificador 'estudiante2' (único: 'estudiante2') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante2
    add x1, x1, :lo12:GV_estudiante2
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    // Asignar identificador 'nota2' (único: 'nota2') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota2
    add x1, x1, :lo12:GV_nota2
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    b endif_27
else_26:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'estudianteActual' (única: 'estudianteActual') en registro x9
    adrp x9, GV_estudianteActual
    add x9, x9, :lo12:GV_estudianteActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_28
    // Bloque IF
    // Asignar identificador 'estudiante3' (único: 'estudiante3') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante3
    add x1, x1, :lo12:GV_estudiante3
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    // Asignar identificador 'nota3' (único: 'nota3') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota3
    add x1, x1, :lo12:GV_nota3
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    b endif_29
else_28:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 4 en registro x8
    mov x8, #4
    str x8, [sp, #-16]!
    // Cargar variable global 'estudianteActual' (única: 'estudianteActual') en registro x9
    adrp x9, GV_estudianteActual
    add x9, x9, :lo12:GV_estudianteActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_30
    // Bloque IF
    // Asignar identificador 'estudiante4' (único: 'estudiante4') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante4
    add x1, x1, :lo12:GV_estudiante4
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    // Asignar identificador 'nota4' (único: 'nota4') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota4
    add x1, x1, :lo12:GV_nota4
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    b endif_31
else_30:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'estudianteActual' (única: 'estudianteActual') en registro x9
    adrp x9, GV_estudianteActual
    add x9, x9, :lo12:GV_estudianteActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq endif_33
    // Bloque IF
    // Asignar identificador 'estudiante5' (único: 'estudiante5') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante5
    add x1, x1, :lo12:GV_estudiante5
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    // Asignar identificador 'nota5' (único: 'nota5') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota5
    add x1, x1, :lo12:GV_nota5
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

endif_33:
    // Fin IF
endif_31:
    // Fin IF
endif_29:
    // Fin IF
endif_27:
    // Fin IF
endif_25:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor=Revisando estudiante 
# # # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor=: 
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=nombreActual
    // Imprimir string literal 'Revisando estudiante '
    adrp x0, STRLIT_23
    add x0, x0, :lo12:STRLIT_23
    mov x1, #0
    bl print_string

    // Cargar variable global 'estudianteActual' (única: 'estudianteActual') en registro x1
    adrp x1, GV_estudianteActual
    add x1, x1, :lo12:GV_estudianteActual
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ': '
    adrp x0, STRLIT_24
    add x0, x0, :lo12:STRLIT_24
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombreActual' (única: 'nombreActual')
    adrp x0, GV_nombreActual
    add x0, x0, :lo12:GV_nombreActual
    // tipo tag=9
    mov x1, #9
    mov x2, #1
    bl print_any_gv

    // ===== Fin System.out.println =====

    // IF statement
    // Cargar entero literal 90 en registro x8
    mov x8, #90
    str x8, [sp, #-16]!
    // Cargar variable global 'notaActual' (única: 'notaActual') en registro x9
    adrp x9, GV_notaActual
    add x9, x9, :lo12:GV_notaActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq endif_35
    // Bloque IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor=Destacado encontrado: 
# # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # #   id nombre=nombreActual
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor= con 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Destacado encontrado: '
    adrp x0, STRLIT_25
    add x0, x0, :lo12:STRLIT_25
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombreActual' (única: 'nombreActual')
    adrp x0, GV_nombreActual
    add x0, x0, :lo12:GV_nombreActual
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' con '
    adrp x0, STRLIT_26
    add x0, x0, :lo12:STRLIT_26
    mov x1, #0
    bl print_string

    // Cargar variable global 'notaActual' (única: 'notaActual') en registro x1
    adrp x1, GV_notaActual
    add x1, x1, :lo12:GV_notaActual
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // Evaluar expresión compleja para 'estudiantesDestacados' (única: 'estudiantesDestacados')
    adrp x11, GV_estudiantesDestacados
    add x11, x11, :lo12:GV_estudiantesDestacados
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'estudiantesDestacados' (única: 'estudiantesDestacados') en registro x9
    adrp x9, GV_estudiantesDestacados
    add x9, x9, :lo12:GV_estudiantesDestacados
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

endif_35:
    // Fin IF
    // Evaluar expresión compleja para 'estudianteActual' (única: 'estudianteActual')
    adrp x11, GV_estudianteActual
    add x11, x11, :lo12:GV_estudianteActual
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'estudianteActual' (única: 'estudianteActual') en registro x9
    adrp x9, GV_estudianteActual
    add x9, x9, :lo12:GV_estudianteActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b while_loop_22
while_end_23:
    // Fin WHILE
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Total de estudiantes destacados: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Total de estudiantes destacados: '
    adrp x0, STRLIT_27
    add x0, x0, :lo12:STRLIT_27
    mov x1, #0
    bl print_string

    // Cargar variable global 'estudiantesDestacados' (única: 'estudiantesDestacados') en registro x1
    adrp x1, GV_estudiantesDestacados
    add x1, x1, :lo12:GV_estudiantesDestacados
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nContador regresivo:
    // Imprimir string literal '\nContador regresivo:'
    adrp x0, STRLIT_28
    add x0, x0, :lo12:STRLIT_28
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con primitivo: contador = 5
    adrp x1, GV_contador
    add x1, x1, :lo12:GV_contador
    mov x2, #5
    str x2, [x1]

    // WHILE statement
while_loop_36:
    // Cargar entero literal 0 en registro x8
    mov x8, #0
    str x8, [sp, #-16]!
    // Cargar variable global 'contador' (única: 'contador') en registro x9
    adrp x9, GV_contador
    add x9, x9, :lo12:GV_contador
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR QUE: comparar x9 > x8
    cmp x9, x8
    cset x9, gt
    cmp x9, #0
    b.eq while_end_37
    // Bloque WHILE
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor=Quedan: 
# # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= estudiantes por procesar
    // Imprimir string literal 'Quedan: '
    adrp x0, STRLIT_29
    add x0, x0, :lo12:STRLIT_29
    mov x1, #0
    bl print_string

    // Cargar variable global 'contador' (única: 'contador') en registro x1
    adrp x1, GV_contador
    add x1, x1, :lo12:GV_contador
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ' estudiantes por procesar'
    adrp x0, STRLIT_30
    add x0, x0, :lo12:STRLIT_30
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Evaluar expresión compleja para 'contador' (única: 'contador')
    adrp x11, GV_contador
    add x11, x11, :lo12:GV_contador
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'contador' (única: 'contador') en registro x9
    adrp x9, GV_contador
    add x9, x9, :lo12:GV_contador
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación RESTA de enteros
    sub x2, x9, x8
    str x2, [x11]

    b while_loop_36
while_end_37:
    // Fin WHILE
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nBusqueda de estudiante joven con buena nota:
    // Imprimir string literal '\nBusqueda de estudiante joven con buena nota:'
    adrp x0, STRLIT_31
    add x0, x0, :lo12:STRLIT_31
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con primitivo: indice = 1
    adrp x1, GV_indice
    add x1, x1, :lo12:GV_indice
    mov x2, #1
    str x2, [x1]

    // WHILE statement
while_loop_38:
    // Cargar entero literal 0 en registro x8
    mov x8, #0
    str x8, [sp, #-16]!
    // Cargar variable global 'estudiantesDestacados' (única: 'estudiantesDestacados') en registro x9
    adrp x9, GV_estudiantesDestacados
    add x9, x9, :lo12:GV_estudiantesDestacados
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR QUE: comparar x9 > x8
    cmp x9, x8
    cset x8, gt
    str x8, [sp, #-16]!
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'indice' (única: 'indice') en registro x9
    adrp x9, GV_indice
    add x9, x9, :lo12:GV_indice
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    ldr x8, [sp], #16
    // Operación AND LÓGICO: x9 && x8
    cmp x9, #0
    csel x9, xzr, x8, eq
    cmp x9, #0
    cset x9, ne
    cmp x9, #0
    b.eq while_end_39
    // Bloque WHILE
    // Declaración con primitivo: nombre = 
    adrp x1, GV_nombre
    add x1, x1, :lo12:GV_nombre
    adrp x2, STRLIT_22
    add x2, x2, :lo12:STRLIT_22
    str x2, [x1]

    // Declaración con primitivo: nota = 0
    adrp x1, GV_nota
    add x1, x1, :lo12:GV_nota
    mov x2, #0
    str x2, [x1]

    // Declaración con primitivo: edad = 0
    adrp x1, GV_edad
    add x1, x1, :lo12:GV_edad
    mov x2, #0
    str x2, [x1]

    // IF statement
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'indice' (única: 'indice') en registro x9
    adrp x9, GV_indice
    add x9, x9, :lo12:GV_indice
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_40
    // Bloque IF
    // Asignar identificador 'estudiante1' (único: 'estudiante1') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante1
    add x1, x1, :lo12:GV_estudiante1
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota1' (único: 'nota1') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota1
    add x1, x1, :lo12:GV_nota1
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

    // Asignar identificador 'edad1' (único: 'edad1') a variable 'edad' (única: 'edad')
    adrp x1, GV_edad1
    add x1, x1, :lo12:GV_edad1
    ldr x2, [x1]
    adrp x3, GV_edad
    add x3, x3, :lo12:GV_edad
    str x2, [x3]

    b endif_41
else_40:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 2 en registro x8
    mov x8, #2
    str x8, [sp, #-16]!
    // Cargar variable global 'indice' (única: 'indice') en registro x9
    adrp x9, GV_indice
    add x9, x9, :lo12:GV_indice
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_42
    // Bloque IF
    // Asignar identificador 'estudiante2' (único: 'estudiante2') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante2
    add x1, x1, :lo12:GV_estudiante2
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota2' (único: 'nota2') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota2
    add x1, x1, :lo12:GV_nota2
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

    // Asignar identificador 'edad2' (único: 'edad2') a variable 'edad' (única: 'edad')
    adrp x1, GV_edad2
    add x1, x1, :lo12:GV_edad2
    ldr x2, [x1]
    adrp x3, GV_edad
    add x3, x3, :lo12:GV_edad
    str x2, [x3]

    b endif_43
else_42:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'indice' (única: 'indice') en registro x9
    adrp x9, GV_indice
    add x9, x9, :lo12:GV_indice
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_44
    // Bloque IF
    // Asignar identificador 'estudiante3' (único: 'estudiante3') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante3
    add x1, x1, :lo12:GV_estudiante3
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota3' (único: 'nota3') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota3
    add x1, x1, :lo12:GV_nota3
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

    // Asignar identificador 'edad3' (único: 'edad3') a variable 'edad' (única: 'edad')
    adrp x1, GV_edad3
    add x1, x1, :lo12:GV_edad3
    ldr x2, [x1]
    adrp x3, GV_edad
    add x3, x3, :lo12:GV_edad
    str x2, [x3]

    b endif_45
else_44:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 4 en registro x8
    mov x8, #4
    str x8, [sp, #-16]!
    // Cargar variable global 'indice' (única: 'indice') en registro x9
    adrp x9, GV_indice
    add x9, x9, :lo12:GV_indice
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_46
    // Bloque IF
    // Asignar identificador 'estudiante4' (único: 'estudiante4') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante4
    add x1, x1, :lo12:GV_estudiante4
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota4' (único: 'nota4') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota4
    add x1, x1, :lo12:GV_nota4
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

    // Asignar identificador 'edad4' (único: 'edad4') a variable 'edad' (única: 'edad')
    adrp x1, GV_edad4
    add x1, x1, :lo12:GV_edad4
    ldr x2, [x1]
    adrp x3, GV_edad
    add x3, x3, :lo12:GV_edad
    str x2, [x3]

    b endif_47
else_46:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'indice' (única: 'indice') en registro x9
    adrp x9, GV_indice
    add x9, x9, :lo12:GV_indice
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq endif_49
    // Bloque IF
    // Asignar identificador 'estudiante5' (único: 'estudiante5') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante5
    add x1, x1, :lo12:GV_estudiante5
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota5' (único: 'nota5') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota5
    add x1, x1, :lo12:GV_nota5
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

    // Asignar identificador 'edad5' (único: 'edad5') a variable 'edad' (única: 'edad')
    adrp x1, GV_edad5
    add x1, x1, :lo12:GV_edad5
    ldr x2, [x1]
    adrp x3, GV_edad
    add x3, x3, :lo12:GV_edad
    str x2, [x3]

endif_49:
    // Fin IF
endif_47:
    // Fin IF
endif_45:
    // Fin IF
endif_43:
    // Fin IF
endif_41:
    // Fin IF
    // IF statement
    // Cargar entero literal 20 en registro x8
    mov x8, #20
    str x8, [sp, #-16]!
    // Cargar variable global 'edad' (única: 'edad') en registro x9
    adrp x9, GV_edad
    add x9, x9, :lo12:GV_edad
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x8, le
    str x8, [sp, #-16]!
    // Cargar entero literal 90 en registro x8
    mov x8, #90
    str x8, [sp, #-16]!
    // Cargar variable global 'nota' (única: 'nota') en registro x9
    adrp x9, GV_nota
    add x9, x9, :lo12:GV_nota
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    ldr x8, [sp], #16
    // Operación AND LÓGICO: x9 && x8
    cmp x9, #0
    csel x9, xzr, x8, eq
    cmp x9, #0
    cset x9, ne
    cmp x9, #0
    b.eq endif_51
    // Bloque IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # #   expresion tipo=0
# # # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # # #   expresion tipo=0
# # # # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # # # #   expresion tipo=0
# # # # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # # # #   prim tipo=9 valor=Estudiante joven destacado: 
# # # # # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # # # # #   id nombre=nombre
# # # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # # #   prim tipo=9 valor= (Edad: 
# # # # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor=, Nota: 
# # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=)
    // Imprimir string literal 'Estudiante joven destacado: '
    adrp x0, STRLIT_32
    add x0, x0, :lo12:STRLIT_32
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombre' (única: 'nombre')
    adrp x0, GV_nombre
    add x0, x0, :lo12:GV_nombre
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' (Edad: '
    adrp x0, STRLIT_33
    add x0, x0, :lo12:STRLIT_33
    mov x1, #0
    bl print_string

    // Cargar variable global 'edad' (única: 'edad') en registro x1
    adrp x1, GV_edad
    add x1, x1, :lo12:GV_edad
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ', Nota: '
    adrp x0, STRLIT_34
    add x0, x0, :lo12:STRLIT_34
    mov x1, #0
    bl print_string

    // Cargar variable global 'nota' (única: 'nota') en registro x1
    adrp x1, GV_nota
    add x1, x1, :lo12:GV_nota
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ')'
    adrp x0, STRLIT_35
    add x0, x0, :lo12:STRLIT_35
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

endif_51:
    // Fin IF
    // Evaluar expresión compleja para 'indice' (única: 'indice')
    adrp x11, GV_indice
    add x11, x11, :lo12:GV_indice
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'indice' (única: 'indice') en registro x9
    adrp x9, GV_indice
    add x9, x9, :lo12:GV_indice
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b while_loop_38
while_end_39:
    // Fin WHILE
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\n--- PROCESAMIENTO CON FOR CLASICO ---
    // Imprimir string literal '\n--- PROCESAMIENTO CON FOR CLASICO ---'
    adrp x0, STRLIT_36
    add x0, x0, :lo12:STRLIT_36
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Listado numerado:
    // Imprimir string literal 'Listado numerado:'
    adrp x0, STRLIT_37
    add x0, x0, :lo12:STRLIT_37
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'i'
    mov x1, #0
    adrp x2, GV_i
    add x2, x2, :lo12:GV_i
    str x1, [x2]
    // Declaración con primitivo: i = 1
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    mov x2, #1
    str x2, [x1]

for_loop_52:
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_54
    // Bloque FOR
    // Declaración con primitivo: nombre = 
    adrp x1, GV_nombre
    add x1, x1, :lo12:GV_nombre
    adrp x2, STRLIT_22
    add x2, x2, :lo12:STRLIT_22
    str x2, [x1]

    // Declaración con primitivo: nota = 0
    adrp x1, GV_nota
    add x1, x1, :lo12:GV_nota
    mov x2, #0
    str x2, [x1]

    // IF statement
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_55
    // Bloque IF
    // Asignar identificador 'estudiante1' (único: 'estudiante1') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante1
    add x1, x1, :lo12:GV_estudiante1
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota1' (único: 'nota1') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota1
    add x1, x1, :lo12:GV_nota1
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

    b endif_56
else_55:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 2 en registro x8
    mov x8, #2
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_57
    // Bloque IF
    // Asignar identificador 'estudiante2' (único: 'estudiante2') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante2
    add x1, x1, :lo12:GV_estudiante2
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota2' (único: 'nota2') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota2
    add x1, x1, :lo12:GV_nota2
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

    b endif_58
else_57:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_59
    // Bloque IF
    // Asignar identificador 'estudiante3' (único: 'estudiante3') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante3
    add x1, x1, :lo12:GV_estudiante3
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota3' (único: 'nota3') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota3
    add x1, x1, :lo12:GV_nota3
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

    b endif_60
else_59:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 4 en registro x8
    mov x8, #4
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_61
    // Bloque IF
    // Asignar identificador 'estudiante4' (único: 'estudiante4') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante4
    add x1, x1, :lo12:GV_estudiante4
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota4' (único: 'nota4') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota4
    add x1, x1, :lo12:GV_nota4
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

    b endif_62
else_61:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq endif_64
    // Bloque IF
    // Asignar identificador 'estudiante5' (único: 'estudiante5') a variable 'nombre' (única: 'nombre')
    adrp x1, GV_estudiante5
    add x1, x1, :lo12:GV_estudiante5
    ldr x2, [x1]
    adrp x3, GV_nombre
    add x3, x3, :lo12:GV_nombre
    str x2, [x3]

    // Asignar identificador 'nota5' (único: 'nota5') a variable 'nota' (única: 'nota')
    adrp x1, GV_nota5
    add x1, x1, :lo12:GV_nota5
    ldr x2, [x1]
    adrp x3, GV_nota
    add x3, x3, :lo12:GV_nota
    str x2, [x3]

endif_64:
    // Fin IF
endif_62:
    // Fin IF
endif_60:
    // Fin IF
endif_58:
    // Fin IF
endif_56:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # #   expresion tipo=0
# # # # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # #   prim tipo=9 valor=. 
# # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # #   id nombre=nombre
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor= - Nota: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Cargar variable global 'i' (única: 'i') en registro x1
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal '. '
    adrp x0, STRLIT_38
    add x0, x0, :lo12:STRLIT_38
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombre' (única: 'nombre')
    adrp x0, GV_nombre
    add x0, x0, :lo12:GV_nombre
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' - Nota: '
    adrp x0, STRLIT_39
    add x0, x0, :lo12:STRLIT_39
    mov x1, #0
    bl print_string

    // Cargar variable global 'nota' (única: 'nota') en registro x1
    adrp x1, GV_nota
    add x1, x1, :lo12:GV_nota
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

for_increment_53:
    // Incremento FOR
    // Evaluar expresión compleja para 'i' (única: 'i')
    adrp x11, GV_i
    add x11, x11, :lo12:GV_i
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_52
for_end_54:
    // Fin FOR
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nProceso inverso:
    // Imprimir string literal '\nProceso inverso:'
    adrp x0, STRLIT_40
    add x0, x0, :lo12:STRLIT_40
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'i'
    mov x1, #0
    adrp x2, GV_i
    add x2, x2, :lo12:GV_i
    str x1, [x2]
    // Declaración con primitivo: i = 5
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    mov x2, #5
    str x2, [x1]

for_loop_65:
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq for_end_67
    // Bloque FOR
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Procesando posicion: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Procesando posicion: '
    adrp x0, STRLIT_41
    add x0, x0, :lo12:STRLIT_41
    mov x1, #0
    bl print_string

    // Cargar variable global 'i' (única: 'i') en registro x1
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

for_increment_66:
    // Incremento FOR
    // Evaluar expresión compleja para 'i' (única: 'i')
    adrp x11, GV_i
    add x11, x11, :lo12:GV_i
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación RESTA de enteros
    sub x2, x9, x8
    str x2, [x11]

    b for_loop_65
for_end_67:
    // Fin FOR
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nTabla de multiplicar (1-3 x 1-4):
    // Imprimir string literal '\nTabla de multiplicar (1-3 x 1-4):'
    adrp x0, STRLIT_42
    add x0, x0, :lo12:STRLIT_42
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'i'
    mov x1, #0
    adrp x2, GV_i
    add x2, x2, :lo12:GV_i
    str x1, [x2]
    // Declaración con primitivo: i = 1
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    mov x2, #1
    str x2, [x1]

for_loop_68:
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_70
    // Bloque FOR
    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'j'
    mov x1, #0
    adrp x2, GV_j
    add x2, x2, :lo12:GV_j
    str x1, [x2]
    // Declaración con primitivo: j = 1
    adrp x1, GV_j
    add x1, x1, :lo12:GV_j
    mov x2, #1
    str x2, [x1]

for_loop_71:
    // Cargar entero literal 4 en registro x8
    mov x8, #4
    str x8, [sp, #-16]!
    // Cargar variable global 'j' (única: 'j') en registro x9
    adrp x9, GV_j
    add x9, x9, :lo12:GV_j
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_73
    // Bloque FOR
    // Declaración con expresión compleja: resultado = expresión
    adrp x11, GV_resultado
    add x11, x11, :lo12:GV_resultado
    // DEBUG: Variable 'resultado' tipo=5
    // Cargar variable global 'j' (única: 'j') en registro x8
    adrp x8, GV_j
    add x8, x8, :lo12:GV_j
    ldr x8, [x8]
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MULTIPLICACIÓN de enteros
    mul x2, x9, x8
    str x2, [x11]

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # #   expresion tipo=0
# # # # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # #   prim tipo=9 valor= x 
# # # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor= = 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Cargar variable global 'i' (única: 'i') en registro x1
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ' x '
    adrp x0, STRLIT_43
    add x0, x0, :lo12:STRLIT_43
    mov x1, #0
    bl print_string

    // Cargar variable global 'j' (única: 'j') en registro x1
    adrp x1, GV_j
    add x1, x1, :lo12:GV_j
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ' = '
    adrp x0, STRLIT_44
    add x0, x0, :lo12:STRLIT_44
    mov x1, #0
    bl print_string

    // Cargar variable global 'resultado' (única: 'resultado') en registro x1
    adrp x1, GV_resultado
    add x1, x1, :lo12:GV_resultado
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

for_increment_72:
    // Incremento FOR
    // Evaluar expresión compleja para 'j' (única: 'j')
    adrp x11, GV_j
    add x11, x11, :lo12:GV_j
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'j' (única: 'j') en registro x9
    adrp x9, GV_j
    add x9, x9, :lo12:GV_j
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_71
for_end_73:
    // Fin FOR
for_increment_69:
    // Incremento FOR
    // Evaluar expresión compleja para 'i' (única: 'i')
    adrp x11, GV_i
    add x11, x11, :lo12:GV_i
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_68
for_end_70:
    // Fin FOR
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\n--- FOREACH CON ARRAY SIMPLE ---
    // Imprimir string literal '\n--- FOREACH CON ARRAY SIMPLE ---'
    adrp x0, STRLIT_45
    add x0, x0, :lo12:STRLIT_45
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Crear array literal 'notasParaForeach' con 5 elementos
    // Asignar memoria para array 'notasParaForeach' (tamaño: 40 bytes)
    mov x0, #40
    bl malloc
    adrp x1, GV_notasParaForeach
    add x1, x1, :lo12:GV_notasParaForeach
    str x0, [x1]
    // Inicializar elemento 0 del array 'notasParaForeach'
    adrp x1, GV_notasParaForeach
    add x1, x1, :lo12:GV_notasParaForeach
    ldr x1, [x1]
    add x1, x1, #0
    mov x2, #85
    str x2, [x1]
    // Inicializar elemento 1 del array 'notasParaForeach'
    adrp x1, GV_notasParaForeach
    add x1, x1, :lo12:GV_notasParaForeach
    ldr x1, [x1]
    add x1, x1, #8
    mov x2, #92
    str x2, [x1]
    // Inicializar elemento 2 del array 'notasParaForeach'
    adrp x1, GV_notasParaForeach
    add x1, x1, :lo12:GV_notasParaForeach
    ldr x1, [x1]
    add x1, x1, #16
    mov x2, #78
    str x2, [x1]
    // Inicializar elemento 3 del array 'notasParaForeach'
    adrp x1, GV_notasParaForeach
    add x1, x1, :lo12:GV_notasParaForeach
    ldr x1, [x1]
    add x1, x1, #24
    mov x2, #95
    str x2, [x1]
    // Inicializar elemento 4 del array 'notasParaForeach'
    adrp x1, GV_notasParaForeach
    add x1, x1, :lo12:GV_notasParaForeach
    ldr x1, [x1]
    add x1, x1, #32
    mov x2, #88
    str x2, [x1]
    // Array 'notasParaForeach' inicializado correctamente
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Procesando notas con forEach:
    // Imprimir string literal 'Procesando notas con forEach:'
    adrp x0, STRLIT_46
    add x0, x0, :lo12:STRLIT_46
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con primitivo: suma = 0
    adrp x1, GV_suma
    add x1, x1, :lo12:GV_suma
    mov x2, #0
    str x2, [x1]

    // Declaración con primitivo: contadorNotas = 0
    adrp x1, GV_contadorNotas
    add x1, x1, :lo12:GV_contadorNotas
    mov x2, #0
    str x2, [x1]

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR '__idx_fe_nota'
    mov x1, #0
    adrp x2, GV___idx_fe_nota
    add x2, x2, :lo12:GV___idx_fe_nota
    str x1, [x2]
    // Declaración con primitivo: __idx_fe_nota = 0
    adrp x1, GV___idx_fe_nota
    add x1, x1, :lo12:GV___idx_fe_nota
    mov x2, #0
    str x2, [x1]

for_loop_74:
    // Obtener longitud total del array 'notasParaForeach'
    mov x8, #5
    // Longitud total del array 'notasParaForeach' en x8
    str x8, [sp, #-16]!
    // Cargar variable global '__idx_fe_nota' (única: '__idx_fe_nota') en registro x9
    adrp x9, GV___idx_fe_nota
    add x9, x9, :lo12:GV___idx_fe_nota
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR QUE: comparar x9 < x8
    cmp x9, x8
    cset x9, lt
    cmp x9, #0
    b.eq for_end_76
    // Bloque FOR
    // Declaración con acceso lineal a array (FOREACH): nota = array[índice]
    // Acceso lineal a array 'notasParaForeach'[índice]
    // Cargar variable global '__idx_fe_nota' (única: '__idx_fe_nota') en registro x9
    adrp x9, GV___idx_fe_nota
    add x9, x9, :lo12:GV___idx_fe_nota
    ldr x9, [x9]
    adrp x1, GV_notasParaForeach
    add x1, x1, :lo12:GV_notasParaForeach
    ldr x1, [x1]
    lsl x9, x9, #3
    add x1, x1, x9
    ldr x9, [x1]
    // Valor del array 'notasParaForeach'[índice] cargado en x9
    adrp x1, GV_nota
    add x1, x1, :lo12:GV_nota
    str x9, [x1]
    // Evaluar expresión compleja para 'contadorNotas' (única: 'contadorNotas')
    adrp x11, GV_contadorNotas
    add x11, x11, :lo12:GV_contadorNotas
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'contadorNotas' (única: 'contadorNotas') en registro x9
    adrp x9, GV_contadorNotas
    add x9, x9, :lo12:GV_contadorNotas
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor=Nota 
# # # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor=: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Nota '
    adrp x0, STRLIT_47
    add x0, x0, :lo12:STRLIT_47
    mov x1, #0
    bl print_string

    // Cargar variable global 'contadorNotas' (única: 'contadorNotas') en registro x1
    adrp x1, GV_contadorNotas
    add x1, x1, :lo12:GV_contadorNotas
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ': '
    adrp x0, STRLIT_24
    add x0, x0, :lo12:STRLIT_24
    mov x1, #0
    bl print_string

    // Cargar variable global 'nota' (única: 'nota') en registro x1
    adrp x1, GV_nota
    add x1, x1, :lo12:GV_nota
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

for_increment_75:
    // Incremento FOR
    // Evaluar expresión compleja para '__idx_fe_nota' (única: '__idx_fe_nota')
    adrp x11, GV___idx_fe_nota
    add x11, x11, :lo12:GV___idx_fe_nota
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global '__idx_fe_nota' (única: '__idx_fe_nota') en registro x9
    adrp x9, GV___idx_fe_nota
    add x9, x9, :lo12:GV___idx_fe_nota
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_74
for_end_76:
    // Fin FOR
    // Declaración con expresión compleja: promedio = expresión
    adrp x11, GV_promedio
    add x11, x11, :lo12:GV_promedio
    // DEBUG: Variable 'promedio' tipo=8
    // Cargar número flotante literal 5.0 en registro d8
    adrp x8, NUMLIT_1
    ldr d8, [x8, :lo12:NUMLIT_1]
    fmov x8, d8
    str x8, [sp, #-16]!
    // Cargar variable global 'suma' (única: 'suma') en registro x9
    adrp x9, GV_suma
    add x9, x9, :lo12:GV_suma
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación DIVISIÓN de double
    // Evaluar operandos directamente para división
    // Cargar variable global 'suma' (única: 'suma') en registro x9
    adrp x9, GV_suma
    add x9, x9, :lo12:GV_suma
    ldr x9, [x9]
    // Convertir entero x9 a double d9
    scvtf d9, x9
    // Cargar literal double 5.0 directamente en d8
    adrp x8, NUMLIT_1
    ldr d8, [x8, :lo12:NUMLIT_1]
    fdiv d2, d9, d8
    // Mantener resultado en d2 para variables double, convertir a x2 para variables enteras
    fcvtns x2, d2
    str d2, [x11]

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Promedio calculado con forEach: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Promedio calculado con forEach: '
    adrp x0, STRLIT_48
    add x0, x0, :lo12:STRLIT_48
    mov x1, #0
    bl print_string

    // Cargar variable double 'promedio' en x1 como bits
    adrp x1, GV_promedio
    ldr x1, [x1, :lo12:GV_promedio]
    mov w0, #2
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nConteo de categorias con forEach:
    // Imprimir string literal '\nConteo de categorias con forEach:'
    adrp x0, STRLIT_49
    add x0, x0, :lo12:STRLIT_49
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con primitivo: notasAltas = 0
    adrp x1, GV_notasAltas
    add x1, x1, :lo12:GV_notasAltas
    mov x2, #0
    str x2, [x1]

    // Declaración con primitivo: notasBajas = 0
    adrp x1, GV_notasBajas
    add x1, x1, :lo12:GV_notasBajas
    mov x2, #0
    str x2, [x1]

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR '__idx_fe_nota'
    mov x1, #0
    adrp x2, GV___idx_fe_nota
    add x2, x2, :lo12:GV___idx_fe_nota
    str x1, [x2]
    // Declaración con primitivo: __idx_fe_nota = 0
    adrp x1, GV___idx_fe_nota
    add x1, x1, :lo12:GV___idx_fe_nota
    mov x2, #0
    str x2, [x1]

for_loop_77:
    // Obtener longitud total del array 'notasParaForeach'
    mov x8, #5
    // Longitud total del array 'notasParaForeach' en x8
    str x8, [sp, #-16]!
    // Cargar variable global '__idx_fe_nota' (única: '__idx_fe_nota') en registro x9
    adrp x9, GV___idx_fe_nota
    add x9, x9, :lo12:GV___idx_fe_nota
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR QUE: comparar x9 < x8
    cmp x9, x8
    cset x9, lt
    cmp x9, #0
    b.eq for_end_79
    // Bloque FOR
    // Declaración con acceso lineal a array (FOREACH): nota = array[índice]
    // Acceso lineal a array 'notasParaForeach'[índice]
    // Cargar variable global '__idx_fe_nota' (única: '__idx_fe_nota') en registro x9
    adrp x9, GV___idx_fe_nota
    add x9, x9, :lo12:GV___idx_fe_nota
    ldr x9, [x9]
    adrp x1, GV_notasParaForeach
    add x1, x1, :lo12:GV_notasParaForeach
    ldr x1, [x1]
    lsl x9, x9, #3
    add x1, x1, x9
    ldr x9, [x1]
    // Valor del array 'notasParaForeach'[índice] cargado en x9
    adrp x1, GV_nota
    add x1, x1, :lo12:GV_nota
    str x9, [x1]
    // IF statement
    // Cargar entero literal 90 en registro x8
    mov x8, #90
    str x8, [sp, #-16]!
    // Cargar variable global 'nota' (única: 'nota') en registro x9
    adrp x9, GV_nota
    add x9, x9, :lo12:GV_nota
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR O IGUAL: comparar x9 >= x8
    cmp x9, x8
    cset x9, ge
    cmp x9, #0
    b.eq else_80
    // Bloque IF
    // Evaluar expresión compleja para 'notasAltas' (única: 'notasAltas')
    adrp x11, GV_notasAltas
    add x11, x11, :lo12:GV_notasAltas
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'notasAltas' (única: 'notasAltas') en registro x9
    adrp x9, GV_notasAltas
    add x9, x9, :lo12:GV_notasAltas
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Nota alta encontrada: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Nota alta encontrada: '
    adrp x0, STRLIT_50
    add x0, x0, :lo12:STRLIT_50
    mov x1, #0
    bl print_string

    // Cargar variable global 'nota' (única: 'nota') en registro x1
    adrp x1, GV_nota
    add x1, x1, :lo12:GV_nota
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    b endif_81
else_80:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 80 en registro x8
    mov x8, #80
    str x8, [sp, #-16]!
    // Cargar variable global 'nota' (única: 'nota') en registro x9
    adrp x9, GV_nota
    add x9, x9, :lo12:GV_nota
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR QUE: comparar x9 < x8
    cmp x9, x8
    cset x9, lt
    cmp x9, #0
    b.eq endif_83
    // Bloque IF
    // Evaluar expresión compleja para 'notasBajas' (única: 'notasBajas')
    adrp x11, GV_notasBajas
    add x11, x11, :lo12:GV_notasBajas
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'notasBajas' (única: 'notasBajas') en registro x9
    adrp x9, GV_notasBajas
    add x9, x9, :lo12:GV_notasBajas
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Nota baja encontrada: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Nota baja encontrada: '
    adrp x0, STRLIT_51
    add x0, x0, :lo12:STRLIT_51
    mov x1, #0
    bl print_string

    // Cargar variable global 'nota' (única: 'nota') en registro x1
    adrp x1, GV_nota
    add x1, x1, :lo12:GV_nota
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

endif_83:
    // Fin IF
endif_81:
    // Fin IF
for_increment_78:
    // Incremento FOR
    // Evaluar expresión compleja para '__idx_fe_nota' (única: '__idx_fe_nota')
    adrp x11, GV___idx_fe_nota
    add x11, x11, :lo12:GV___idx_fe_nota
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global '__idx_fe_nota' (única: '__idx_fe_nota') en registro x9
    adrp x9, GV___idx_fe_nota
    add x9, x9, :lo12:GV___idx_fe_nota
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_77
for_end_79:
    // Fin FOR
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Total notas altas (>=90): 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Total notas altas (>=90): '
    adrp x0, STRLIT_52
    add x0, x0, :lo12:STRLIT_52
    mov x1, #0
    bl print_string

    // Cargar variable global 'notasAltas' (única: 'notasAltas') en registro x1
    adrp x1, GV_notasAltas
    add x1, x1, :lo12:GV_notasAltas
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Total notas bajas (<80): 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Total notas bajas (<80): '
    adrp x0, STRLIT_53
    add x0, x0, :lo12:STRLIT_53
    mov x1, #0
    bl print_string

    // Cargar variable global 'notasBajas' (única: 'notasBajas') en registro x1
    adrp x1, GV_notasBajas
    add x1, x1, :lo12:GV_notasBajas
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\n--- CLASIFICACION CON SWITCH/CASE ---
    // Imprimir string literal '\n--- CLASIFICACION CON SWITCH/CASE ---'
    adrp x0, STRLIT_54
    add x0, x0, :lo12:STRLIT_54
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # #   expresion tipo=0
# # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # #   prim tipo=9 valor=Analizando: 
# # # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # # #   id nombre=estudiante1
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor= (Nota: 
# # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=)
    // Imprimir string literal 'Analizando: '
    adrp x0, STRLIT_55
    add x0, x0, :lo12:STRLIT_55
    mov x1, #0
    bl print_string

    // Imprimir variable global 'estudiante1' (única: 'estudiante1')
    adrp x0, GV_estudiante1
    add x0, x0, :lo12:GV_estudiante1
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' (Nota: '
    adrp x0, STRLIT_56
    add x0, x0, :lo12:STRLIT_56
    mov x1, #0
    bl print_string

    // Cargar variable global 'nota1' (única: 'nota1') en registro x1
    adrp x1, GV_nota1
    add x1, x1, :lo12:GV_nota1
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ')'
    adrp x0, STRLIT_35
    add x0, x0, :lo12:STRLIT_35
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con expresión compleja: categoria1 = expresión
    adrp x11, GV_categoria1
    add x11, x11, :lo12:GV_categoria1
    // DEBUG: Variable 'categoria1' tipo=5
    // Cargar entero literal 10 en registro x8
    mov x8, #10
    str x8, [sp, #-16]!
    // Cargar variable global 'nota1' (única: 'nota1') en registro x9
    adrp x9, GV_nota1
    add x9, x9, :lo12:GV_nota1
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación DIVISIÓN de double
    // Evaluar operandos directamente para división
    // Cargar variable global 'nota1' (única: 'nota1') en registro x9
    adrp x9, GV_nota1
    add x9, x9, :lo12:GV_nota1
    ldr x9, [x9]
    // Convertir entero x9 a double d9
    scvtf d9, x9
    // Cargar entero literal 10 en registro x8
    mov x8, #10
    // Convertir entero x8 a double d8
    scvtf d8, x8
    fdiv d2, d9, d8
    // Mantener resultado en d2 para variables double, convertir a x2 para variables enteras
    fcvtns x2, d2
    str x2, [x11]

    // SWITCH statement
    // Cargar variable global 'categoria1' (única: 'categoria1') en registro x9
    adrp x9, GV_categoria1
    add x9, x9, :lo12:GV_categoria1
    ldr x9, [x9]
    // Cargar entero literal 10 en registro x8
    mov x8, #10
    cmp x9, x8
    b.eq case_85
    b next_case_86
case_85:
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: EXCELENTE
    // Imprimir string literal 'Categoria: EXCELENTE'
    adrp x0, STRLIT_57
    add x0, x0, :lo12:STRLIT_57
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Accion: Mantener nivel
    // Imprimir string literal 'Accion: Mantener nivel'
    adrp x0, STRLIT_58
    add x0, x0, :lo12:STRLIT_58
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_84
next_case_86:
    // Cargar entero literal 9 en registro x8
    mov x8, #9
    cmp x9, x8
    b.eq case_87
    b next_case_88
case_87:
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: EXCELENTE
    // Imprimir string literal 'Categoria: EXCELENTE'
    adrp x0, STRLIT_57
    add x0, x0, :lo12:STRLIT_57
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Accion: Mantener nivel
    // Imprimir string literal 'Accion: Mantener nivel'
    adrp x0, STRLIT_58
    add x0, x0, :lo12:STRLIT_58
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_84
next_case_88:
    // Cargar entero literal 8 en registro x8
    mov x8, #8
    cmp x9, x8
    b.eq case_89
    b next_case_90
case_89:
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: MUY BUENO
    // Imprimir string literal 'Categoria: MUY BUENO'
    adrp x0, STRLIT_59
    add x0, x0, :lo12:STRLIT_59
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Accion: Continuar esfuerzo
    // Imprimir string literal 'Accion: Continuar esfuerzo'
    adrp x0, STRLIT_60
    add x0, x0, :lo12:STRLIT_60
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_84
next_case_90:
    // Cargar entero literal 7 en registro x8
    mov x8, #7
    cmp x9, x8
    b.eq case_91
    b next_case_92
case_91:
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: BUENO
    // Imprimir string literal 'Categoria: BUENO'
    adrp x0, STRLIT_61
    add x0, x0, :lo12:STRLIT_61
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Accion: Mejorar tecnicas
    // Imprimir string literal 'Accion: Mejorar tecnicas'
    adrp x0, STRLIT_62
    add x0, x0, :lo12:STRLIT_62
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_84
next_case_92:
    // Cargar entero literal 6 en registro x8
    mov x8, #6
    cmp x9, x8
    b.eq case_93
    b next_case_94
case_93:
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: SUFICIENTE
    // Imprimir string literal 'Categoria: SUFICIENTE'
    adrp x0, STRLIT_63
    add x0, x0, :lo12:STRLIT_63
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Accion: Reforzar conocimientos
    // Imprimir string literal 'Accion: Reforzar conocimientos'
    adrp x0, STRLIT_64
    add x0, x0, :lo12:STRLIT_64
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_84
next_case_94:
    // DEFAULT case
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: INSUFICIENTE
    // Imprimir string literal 'Categoria: INSUFICIENTE'
    adrp x0, STRLIT_65
    add x0, x0, :lo12:STRLIT_65
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Accion: Apoyo urgente
    // Imprimir string literal 'Accion: Apoyo urgente'
    adrp x0, STRLIT_66
    add x0, x0, :lo12:STRLIT_66
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

switch_end_84:
    // Fin SWITCH
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # #   expresion tipo=0
# # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # #   prim tipo=9 valor=\nAnalizando: 
# # # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # # #   id nombre=estudiante2
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor= (Nota: 
# # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=)
    // Imprimir string literal '\nAnalizando: '
    adrp x0, STRLIT_67
    add x0, x0, :lo12:STRLIT_67
    mov x1, #0
    bl print_string

    // Imprimir variable global 'estudiante2' (única: 'estudiante2')
    adrp x0, GV_estudiante2
    add x0, x0, :lo12:GV_estudiante2
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' (Nota: '
    adrp x0, STRLIT_56
    add x0, x0, :lo12:STRLIT_56
    mov x1, #0
    bl print_string

    // Cargar variable global 'nota2' (única: 'nota2') en registro x1
    adrp x1, GV_nota2
    add x1, x1, :lo12:GV_nota2
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ')'
    adrp x0, STRLIT_35
    add x0, x0, :lo12:STRLIT_35
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con expresión compleja: categoria2 = expresión
    adrp x11, GV_categoria2
    add x11, x11, :lo12:GV_categoria2
    // DEBUG: Variable 'categoria2' tipo=5
    // Cargar entero literal 10 en registro x8
    mov x8, #10
    str x8, [sp, #-16]!
    // Cargar variable global 'nota2' (única: 'nota2') en registro x9
    adrp x9, GV_nota2
    add x9, x9, :lo12:GV_nota2
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación DIVISIÓN de double
    // Evaluar operandos directamente para división
    // Cargar variable global 'nota2' (única: 'nota2') en registro x9
    adrp x9, GV_nota2
    add x9, x9, :lo12:GV_nota2
    ldr x9, [x9]
    // Convertir entero x9 a double d9
    scvtf d9, x9
    // Cargar entero literal 10 en registro x8
    mov x8, #10
    // Convertir entero x8 a double d8
    scvtf d8, x8
    fdiv d2, d9, d8
    // Mantener resultado en d2 para variables double, convertir a x2 para variables enteras
    fcvtns x2, d2
    str x2, [x11]

    // SWITCH statement
    // Cargar variable global 'categoria2' (única: 'categoria2') en registro x9
    adrp x9, GV_categoria2
    add x9, x9, :lo12:GV_categoria2
    ldr x9, [x9]
    // Cargar entero literal 10 en registro x8
    mov x8, #10
    cmp x9, x8
    b.eq case_96
    b next_case_97
case_96:
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: EXCELENTE
    // Imprimir string literal 'Categoria: EXCELENTE'
    adrp x0, STRLIT_57
    add x0, x0, :lo12:STRLIT_57
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_95
next_case_97:
    // Cargar entero literal 9 en registro x8
    mov x8, #9
    cmp x9, x8
    b.eq case_98
    b next_case_99
case_98:
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: EXCELENTE
    // Imprimir string literal 'Categoria: EXCELENTE'
    adrp x0, STRLIT_57
    add x0, x0, :lo12:STRLIT_57
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_95
next_case_99:
    // Cargar entero literal 8 en registro x8
    mov x8, #8
    cmp x9, x8
    b.eq case_100
    b next_case_101
case_100:
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: MUY BUENO
    // Imprimir string literal 'Categoria: MUY BUENO'
    adrp x0, STRLIT_59
    add x0, x0, :lo12:STRLIT_59
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_95
next_case_101:
    // Cargar entero literal 7 en registro x8
    mov x8, #7
    cmp x9, x8
    b.eq case_102
    b next_case_103
case_102:
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: BUENO
    // Imprimir string literal 'Categoria: BUENO'
    adrp x0, STRLIT_61
    add x0, x0, :lo12:STRLIT_61
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_95
next_case_103:
    // DEFAULT case
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Categoria: REGULAR
    // Imprimir string literal 'Categoria: REGULAR'
    adrp x0, STRLIT_68
    add x0, x0, :lo12:STRLIT_68
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

switch_end_95:
    // Fin SWITCH
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nAnalisis por edad:
    // Imprimir string literal '\nAnalisis por edad:'
    adrp x0, STRLIT_69
    add x0, x0, :lo12:STRLIT_69
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // SWITCH statement
    // Cargar variable global 'edad1' (única: 'edad1') en registro x9
    adrp x9, GV_edad1
    add x9, x9, :lo12:GV_edad1
    ldr x9, [x9]
    // Cargar entero literal 18 en registro x8
    mov x8, #18
    cmp x9, x8
    b.eq case_105
    b next_case_106
case_105:
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante1
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= - Categoria: Muy joven
    // Imprimir variable global 'estudiante1' (única: 'estudiante1')
    adrp x0, GV_estudiante1
    add x0, x0, :lo12:GV_estudiante1
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' - Categoria: Muy joven'
    adrp x0, STRLIT_70
    add x0, x0, :lo12:STRLIT_70
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_104
next_case_106:
    // Cargar entero literal 19 en registro x8
    mov x8, #19
    cmp x9, x8
    b.eq case_107
    b next_case_108
case_107:
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante1
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= - Categoria: Muy joven
    // Imprimir variable global 'estudiante1' (única: 'estudiante1')
    adrp x0, GV_estudiante1
    add x0, x0, :lo12:GV_estudiante1
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' - Categoria: Muy joven'
    adrp x0, STRLIT_70
    add x0, x0, :lo12:STRLIT_70
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_104
next_case_108:
    // Cargar entero literal 20 en registro x8
    mov x8, #20
    cmp x9, x8
    b.eq case_109
    b next_case_110
case_109:
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante1
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= - Categoria: Joven adulto
    // Imprimir variable global 'estudiante1' (única: 'estudiante1')
    adrp x0, GV_estudiante1
    add x0, x0, :lo12:GV_estudiante1
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' - Categoria: Joven adulto'
    adrp x0, STRLIT_71
    add x0, x0, :lo12:STRLIT_71
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_104
next_case_110:
    // Cargar entero literal 21 en registro x8
    mov x8, #21
    cmp x9, x8
    b.eq case_111
    b next_case_112
case_111:
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante1
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= - Categoria: Joven adulto
    // Imprimir variable global 'estudiante1' (única: 'estudiante1')
    adrp x0, GV_estudiante1
    add x0, x0, :lo12:GV_estudiante1
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' - Categoria: Joven adulto'
    adrp x0, STRLIT_71
    add x0, x0, :lo12:STRLIT_71
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_104
next_case_112:
    // Cargar entero literal 22 en registro x8
    mov x8, #22
    cmp x9, x8
    b.eq case_113
    b next_case_114
case_113:
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante1
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= - Categoria: Adulto joven
    // Imprimir variable global 'estudiante1' (única: 'estudiante1')
    adrp x0, GV_estudiante1
    add x0, x0, :lo12:GV_estudiante1
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' - Categoria: Adulto joven'
    adrp x0, STRLIT_72
    add x0, x0, :lo12:STRLIT_72
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_104
next_case_114:
    // Cargar entero literal 23 en registro x8
    mov x8, #23
    cmp x9, x8
    b.eq case_115
    b next_case_116
case_115:
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante1
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= - Categoria: Adulto joven
    // Imprimir variable global 'estudiante1' (única: 'estudiante1')
    adrp x0, GV_estudiante1
    add x0, x0, :lo12:GV_estudiante1
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' - Categoria: Adulto joven'
    adrp x0, STRLIT_72
    add x0, x0, :lo12:STRLIT_72
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    b switch_end_104
next_case_116:
    // DEFAULT case
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# #   id nombre=estudiante1
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= - Categoria: Adulto
    // Imprimir variable global 'estudiante1' (única: 'estudiante1')
    adrp x0, GV_estudiante1
    add x0, x0, :lo12:GV_estudiante1
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' - Categoria: Adulto'
    adrp x0, STRLIT_73
    add x0, x0, :lo12:STRLIT_73
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

switch_end_104:
    // Fin SWITCH
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\n--- DEMOSTRACION DE BREAK ---
    // Imprimir string literal '\n--- DEMOSTRACION DE BREAK ---'
    adrp x0, STRLIT_74
    add x0, x0, :lo12:STRLIT_74
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Busqueda con break (primera nota > 90):
    // Imprimir string literal 'Busqueda con break (primera nota > 90):'
    adrp x0, STRLIT_75
    add x0, x0, :lo12:STRLIT_75
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'i'
    mov x1, #0
    adrp x2, GV_i
    add x2, x2, :lo12:GV_i
    str x1, [x2]
    // Declaración con primitivo: i = 1
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    mov x2, #1
    str x2, [x1]

for_loop_117:
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_119
    // Bloque FOR
    // Declaración con primitivo: notaActual = 0
    adrp x1, GV_notaActual
    add x1, x1, :lo12:GV_notaActual
    mov x2, #0
    str x2, [x1]

    // Declaración con primitivo: nombreActual = 
    adrp x1, GV_nombreActual
    add x1, x1, :lo12:GV_nombreActual
    adrp x2, STRLIT_22
    add x2, x2, :lo12:STRLIT_22
    str x2, [x1]

    // IF statement
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_120
    // Bloque IF
    // Asignar identificador 'nota1' (único: 'nota1') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota1
    add x1, x1, :lo12:GV_nota1
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante1' (único: 'estudiante1') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante1
    add x1, x1, :lo12:GV_estudiante1
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    b endif_121
else_120:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 2 en registro x8
    mov x8, #2
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_122
    // Bloque IF
    // Asignar identificador 'nota2' (único: 'nota2') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota2
    add x1, x1, :lo12:GV_nota2
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante2' (único: 'estudiante2') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante2
    add x1, x1, :lo12:GV_estudiante2
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    b endif_123
else_122:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_124
    // Bloque IF
    // Asignar identificador 'nota3' (único: 'nota3') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota3
    add x1, x1, :lo12:GV_nota3
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante3' (único: 'estudiante3') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante3
    add x1, x1, :lo12:GV_estudiante3
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    b endif_125
else_124:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 4 en registro x8
    mov x8, #4
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_126
    // Bloque IF
    // Asignar identificador 'nota4' (único: 'nota4') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota4
    add x1, x1, :lo12:GV_nota4
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante4' (único: 'estudiante4') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante4
    add x1, x1, :lo12:GV_estudiante4
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    b endif_127
else_126:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq endif_129
    // Bloque IF
    // Asignar identificador 'nota5' (único: 'nota5') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota5
    add x1, x1, :lo12:GV_nota5
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante5' (único: 'estudiante5') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante5
    add x1, x1, :lo12:GV_estudiante5
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

endif_129:
    // Fin IF
endif_127:
    // Fin IF
endif_125:
    // Fin IF
endif_123:
    // Fin IF
endif_121:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor=Verificando: 
# # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # #   id nombre=nombreActual
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor= - 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Verificando: '
    adrp x0, STRLIT_76
    add x0, x0, :lo12:STRLIT_76
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombreActual' (única: 'nombreActual')
    adrp x0, GV_nombreActual
    add x0, x0, :lo12:GV_nombreActual
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' - '
    adrp x0, STRLIT_77
    add x0, x0, :lo12:STRLIT_77
    mov x1, #0
    bl print_string

    // Cargar variable global 'notaActual' (única: 'notaActual') en registro x1
    adrp x1, GV_notaActual
    add x1, x1, :lo12:GV_notaActual
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // IF statement
    // Cargar entero literal 90 en registro x8
    mov x8, #90
    str x8, [sp, #-16]!
    // Cargar variable global 'notaActual' (única: 'notaActual') en registro x9
    adrp x9, GV_notaActual
    add x9, x9, :lo12:GV_notaActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR QUE: comparar x9 > x8
    cmp x9, x8
    cset x9, gt
    cmp x9, #0
    b.eq endif_131
    // Bloque IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor=Encontrado! 
# # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # #   id nombre=nombreActual
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= supera 90 puntos
    // Imprimir string literal 'Encontrado! '
    adrp x0, STRLIT_78
    add x0, x0, :lo12:STRLIT_78
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombreActual' (única: 'nombreActual')
    adrp x0, GV_nombreActual
    add x0, x0, :lo12:GV_nombreActual
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' supera 90 puntos'
    adrp x0, STRLIT_79
    add x0, x0, :lo12:STRLIT_79
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // BREAK statement - saltar al final del bucle
    b for_end_119
endif_131:
    // Fin IF
for_increment_118:
    // Incremento FOR
    // Evaluar expresión compleja para 'i' (única: 'i')
    adrp x11, GV_i
    add x11, x11, :lo12:GV_i
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_117
for_end_119:
    // Fin FOR
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nBusqueda limitada con while y break:
    // Imprimir string literal '\nBusqueda limitada con while y break:'
    adrp x0, STRLIT_80
    add x0, x0, :lo12:STRLIT_80
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con primitivo: posicion = 1
    adrp x1, GV_posicion
    add x1, x1, :lo12:GV_posicion
    mov x2, #1
    str x2, [x1]

    // WHILE statement
while_loop_132:
    // Cargar booleano literal true en registro x9
    mov x9, #1
    cmp x9, #0
    b.eq while_end_133
    // Bloque WHILE
    // IF statement
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'posicion' (única: 'posicion') en registro x9
    adrp x9, GV_posicion
    add x9, x9, :lo12:GV_posicion
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR QUE: comparar x9 > x8
    cmp x9, x8
    cset x9, gt
    cmp x9, #0
    b.eq endif_135
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Limite alcanzado, usando break para salir
    // Imprimir string literal 'Limite alcanzado, usando break para salir'
    adrp x0, STRLIT_81
    add x0, x0, :lo12:STRLIT_81
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // BREAK statement - saltar al final del bucle
    b while_end_133
endif_135:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Procesando posicion: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Procesando posicion: '
    adrp x0, STRLIT_41
    add x0, x0, :lo12:STRLIT_41
    mov x1, #0
    bl print_string

    // Cargar variable global 'posicion' (única: 'posicion') en registro x1
    adrp x1, GV_posicion
    add x1, x1, :lo12:GV_posicion
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // Evaluar expresión compleja para 'posicion' (única: 'posicion')
    adrp x11, GV_posicion
    add x11, x11, :lo12:GV_posicion
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'posicion' (única: 'posicion') en registro x9
    adrp x9, GV_posicion
    add x9, x9, :lo12:GV_posicion
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b while_loop_132
while_end_133:
    // Fin WHILE
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nBreak en for anidado:
    // Imprimir string literal '\nBreak en for anidado:'
    adrp x0, STRLIT_82
    add x0, x0, :lo12:STRLIT_82
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'i'
    mov x1, #0
    adrp x2, GV_i
    add x2, x2, :lo12:GV_i
    str x1, [x2]
    // Declaración con primitivo: i = 1
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    mov x2, #1
    str x2, [x1]

for_loop_136:
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_138
    // Bloque FOR
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor=Fila 
# # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=:
    // Imprimir string literal 'Fila '
    adrp x0, STRLIT_83
    add x0, x0, :lo12:STRLIT_83
    mov x1, #0
    bl print_string

    // Cargar variable global 'i' (única: 'i') en registro x1
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ':'
    adrp x0, STRLIT_84
    add x0, x0, :lo12:STRLIT_84
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'j'
    mov x1, #0
    adrp x2, GV_j
    add x2, x2, :lo12:GV_j
    str x1, [x2]
    // Declaración con primitivo: j = 1
    adrp x1, GV_j
    add x1, x1, :lo12:GV_j
    mov x2, #1
    str x2, [x1]

for_loop_139:
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'j' (única: 'j') en registro x9
    adrp x9, GV_j
    add x9, x9, :lo12:GV_j
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_141
    // Bloque FOR
    // IF statement
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'j' (única: 'j') en registro x9
    adrp x9, GV_j
    add x9, x9, :lo12:GV_j
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq endif_143
    // Bloque IF
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=  Break en columna 3
    // Imprimir string literal '  Break en columna 3'
    adrp x0, STRLIT_85
    add x0, x0, :lo12:STRLIT_85
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // BREAK statement - saltar al final del bucle
    b for_end_141
endif_143:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=  Columna 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal '  Columna '
    adrp x0, STRLIT_86
    add x0, x0, :lo12:STRLIT_86
    mov x1, #0
    bl print_string

    // Cargar variable global 'j' (única: 'j') en registro x1
    adrp x1, GV_j
    add x1, x1, :lo12:GV_j
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

for_increment_140:
    // Incremento FOR
    // Evaluar expresión compleja para 'j' (única: 'j')
    adrp x11, GV_j
    add x11, x11, :lo12:GV_j
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'j' (única: 'j') en registro x9
    adrp x9, GV_j
    add x9, x9, :lo12:GV_j
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_139
for_end_141:
    // Fin FOR
for_increment_137:
    // Incremento FOR
    // Evaluar expresión compleja para 'i' (única: 'i')
    adrp x11, GV_i
    add x11, x11, :lo12:GV_i
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_136
for_end_138:
    // Fin FOR
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\n--- DEMOSTRACION DE CONTINUE ---
    // Imprimir string literal '\n--- DEMOSTRACION DE CONTINUE ---'
    adrp x0, STRLIT_87
    add x0, x0, :lo12:STRLIT_87
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=Procesando solo notas pares (con continue):
    // Imprimir string literal 'Procesando solo notas pares (con continue):'
    adrp x0, STRLIT_88
    add x0, x0, :lo12:STRLIT_88
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'i'
    mov x1, #0
    adrp x2, GV_i
    add x2, x2, :lo12:GV_i
    str x1, [x2]
    // Declaración con primitivo: i = 1
    adrp x1, GV_i
    add x1, x1, :lo12:GV_i
    mov x2, #1
    str x2, [x1]

for_loop_144:
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_146
    // Bloque FOR
    // Declaración con primitivo: notaActual = 0
    adrp x1, GV_notaActual
    add x1, x1, :lo12:GV_notaActual
    mov x2, #0
    str x2, [x1]

    // Declaración con primitivo: nombreActual = 
    adrp x1, GV_nombreActual
    add x1, x1, :lo12:GV_nombreActual
    adrp x2, STRLIT_22
    add x2, x2, :lo12:STRLIT_22
    str x2, [x1]

    // IF statement
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_147
    // Bloque IF
    // Asignar identificador 'nota1' (único: 'nota1') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota1
    add x1, x1, :lo12:GV_nota1
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante1' (único: 'estudiante1') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante1
    add x1, x1, :lo12:GV_estudiante1
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    b endif_148
else_147:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 2 en registro x8
    mov x8, #2
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_149
    // Bloque IF
    // Asignar identificador 'nota2' (único: 'nota2') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota2
    add x1, x1, :lo12:GV_nota2
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante2' (único: 'estudiante2') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante2
    add x1, x1, :lo12:GV_estudiante2
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    b endif_150
else_149:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_151
    // Bloque IF
    // Asignar identificador 'nota3' (único: 'nota3') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota3
    add x1, x1, :lo12:GV_nota3
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante3' (único: 'estudiante3') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante3
    add x1, x1, :lo12:GV_estudiante3
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    b endif_152
else_151:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 4 en registro x8
    mov x8, #4
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_153
    // Bloque IF
    // Asignar identificador 'nota4' (único: 'nota4') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota4
    add x1, x1, :lo12:GV_nota4
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante4' (único: 'estudiante4') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante4
    add x1, x1, :lo12:GV_estudiante4
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

    b endif_154
else_153:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq endif_156
    // Bloque IF
    // Asignar identificador 'nota5' (único: 'nota5') a variable 'notaActual' (única: 'notaActual')
    adrp x1, GV_nota5
    add x1, x1, :lo12:GV_nota5
    ldr x2, [x1]
    adrp x3, GV_notaActual
    add x3, x3, :lo12:GV_notaActual
    str x2, [x3]

    // Asignar identificador 'estudiante5' (único: 'estudiante5') a variable 'nombreActual' (única: 'nombreActual')
    adrp x1, GV_estudiante5
    add x1, x1, :lo12:GV_estudiante5
    ldr x2, [x1]
    adrp x3, GV_nombreActual
    add x3, x3, :lo12:GV_nombreActual
    str x2, [x3]

endif_156:
    // Fin IF
endif_154:
    // Fin IF
endif_152:
    // Fin IF
endif_150:
    // Fin IF
endif_148:
    // Fin IF
    // IF statement
    // Cargar entero literal 0 en registro x8
    mov x8, #0
    str x8, [sp, #-16]!
    // Cargar entero literal 2 en registro x8
    mov x8, #2
    str x8, [sp, #-16]!
    // Cargar variable global 'notaActual' (única: 'notaActual') en registro x9
    adrp x9, GV_notaActual
    add x9, x9, :lo12:GV_notaActual
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MÓDULO: llamar helper C para módulo
    mov x0, x9
    mov x1, x8
    bl mod_helper
    mov x9, x0
    ldr x8, [sp], #16
    // Operación DESIGUALDAD: comparar x9 != x8
    cmp x9, x8
    cset x9, ne
    cmp x9, #0
    b.eq endif_158
    // Bloque IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # #   expresion tipo=0
# # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # #   prim tipo=9 valor=Saltando 
# # # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # # #   id nombre=nombreActual
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor= (nota impar: 
# # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=)
    // Imprimir string literal 'Saltando '
    adrp x0, STRLIT_89
    add x0, x0, :lo12:STRLIT_89
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombreActual' (única: 'nombreActual')
    adrp x0, GV_nombreActual
    add x0, x0, :lo12:GV_nombreActual
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' (nota impar: '
    adrp x0, STRLIT_90
    add x0, x0, :lo12:STRLIT_90
    mov x1, #0
    bl print_string

    // Cargar variable global 'notaActual' (única: 'notaActual') en registro x1
    adrp x1, GV_notaActual
    add x1, x1, :lo12:GV_notaActual
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ')'
    adrp x0, STRLIT_35
    add x0, x0, :lo12:STRLIT_35
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // CONTINUE statement - saltar al inicio del bucle
    b for_increment_145
endif_158:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # #   expresion tipo=0
# # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # #   prim tipo=9 valor=Procesando 
# # # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # # #   id nombre=nombreActual
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor= (nota par: 
# # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=)
    // Imprimir string literal 'Procesando '
    adrp x0, STRLIT_91
    add x0, x0, :lo12:STRLIT_91
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombreActual' (única: 'nombreActual')
    adrp x0, GV_nombreActual
    add x0, x0, :lo12:GV_nombreActual
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' (nota par: '
    adrp x0, STRLIT_92
    add x0, x0, :lo12:STRLIT_92
    mov x1, #0
    bl print_string

    // Cargar variable global 'notaActual' (única: 'notaActual') en registro x1
    adrp x1, GV_notaActual
    add x1, x1, :lo12:GV_notaActual
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ')'
    adrp x0, STRLIT_35
    add x0, x0, :lo12:STRLIT_35
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

for_increment_145:
    // Incremento FOR
    // Evaluar expresión compleja para 'i' (única: 'i')
    adrp x11, GV_i
    add x11, x11, :lo12:GV_i
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'i' (única: 'i') en registro x9
    adrp x9, GV_i
    add x9, x9, :lo12:GV_i
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_144
for_end_146:
    // Fin FOR
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nConteo con continue (saltando multiplos de 3):
    // Imprimir string literal '\nConteo con continue (saltando multiplos de 3):'
    adrp x0, STRLIT_93
    add x0, x0, :lo12:STRLIT_93
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con primitivo: numero = 0
    adrp x1, GV_numero
    add x1, x1, :lo12:GV_numero
    mov x2, #0
    str x2, [x1]

    // WHILE statement
while_loop_159:
    // Cargar entero literal 10 en registro x8
    mov x8, #10
    str x8, [sp, #-16]!
    // Cargar variable global 'numero' (única: 'numero') en registro x9
    adrp x9, GV_numero
    add x9, x9, :lo12:GV_numero
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR QUE: comparar x9 < x8
    cmp x9, x8
    cset x9, lt
    cmp x9, #0
    b.eq while_end_160
    // Bloque WHILE
    // Evaluar expresión compleja para 'numero' (única: 'numero')
    adrp x11, GV_numero
    add x11, x11, :lo12:GV_numero
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'numero' (única: 'numero') en registro x9
    adrp x9, GV_numero
    add x9, x9, :lo12:GV_numero
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    // IF statement
    // Cargar entero literal 0 en registro x8
    mov x8, #0
    str x8, [sp, #-16]!
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'numero' (única: 'numero') en registro x9
    adrp x9, GV_numero
    add x9, x9, :lo12:GV_numero
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MÓDULO: llamar helper C para módulo
    mov x0, x9
    mov x1, x8
    bl mod_helper
    mov x9, x0
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq endif_162
    // Bloque IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Saltando multiplo de 3: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Saltando multiplo de 3: '
    adrp x0, STRLIT_94
    add x0, x0, :lo12:STRLIT_94
    mov x1, #0
    bl print_string

    // Cargar variable global 'numero' (única: 'numero') en registro x1
    adrp x1, GV_numero
    add x1, x1, :lo12:GV_numero
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // CONTINUE statement - saltar al inicio del bucle
    b while_loop_159
endif_162:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Numero procesado: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Numero procesado: '
    adrp x0, STRLIT_95
    add x0, x0, :lo12:STRLIT_95
    mov x1, #0
    bl print_string

    // Cargar variable global 'numero' (única: 'numero') en registro x1
    adrp x1, GV_numero
    add x1, x1, :lo12:GV_numero
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    b while_loop_159
while_end_160:
    // Fin WHILE
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nMatriz con continue anidado:
    // Imprimir string literal '\nMatriz con continue anidado:'
    adrp x0, STRLIT_96
    add x0, x0, :lo12:STRLIT_96
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'fila'
    mov x1, #0
    adrp x2, GV_fila
    add x2, x2, :lo12:GV_fila
    str x1, [x2]
    // Declaración con primitivo: fila = 1
    adrp x1, GV_fila
    add x1, x1, :lo12:GV_fila
    mov x2, #1
    str x2, [x1]

for_loop_163:
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'fila' (única: 'fila') en registro x9
    adrp x9, GV_fila
    add x9, x9, :lo12:GV_fila
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_165
    // Bloque FOR
    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'columna'
    mov x1, #0
    adrp x2, GV_columna
    add x2, x2, :lo12:GV_columna
    str x1, [x2]
    // Declaración con primitivo: columna = 1
    adrp x1, GV_columna
    add x1, x1, :lo12:GV_columna
    mov x2, #1
    str x2, [x1]

for_loop_166:
    // Cargar entero literal 4 en registro x8
    mov x8, #4
    str x8, [sp, #-16]!
    // Cargar variable global 'columna' (única: 'columna') en registro x9
    adrp x9, GV_columna
    add x9, x9, :lo12:GV_columna
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_168
    // Bloque FOR
    // IF statement
    // Cargar entero literal 2 en registro x8
    mov x8, #2
    str x8, [sp, #-16]!
    // Cargar variable global 'columna' (única: 'columna') en registro x9
    adrp x9, GV_columna
    add x9, x9, :lo12:GV_columna
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq endif_170
    // Bloque IF
    // CONTINUE statement - saltar al inicio del bucle
    b for_increment_167
endif_170:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # #   expresion tipo=0
# # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # #   prim tipo=9 valor=Celda [
# # # # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor=,
# # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=]
    // Imprimir string literal 'Celda ['
    adrp x0, STRLIT_97
    add x0, x0, :lo12:STRLIT_97
    mov x1, #0
    bl print_string

    // Cargar variable global 'fila' (única: 'fila') en registro x1
    adrp x1, GV_fila
    add x1, x1, :lo12:GV_fila
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ','
    adrp x0, STRLIT_98
    add x0, x0, :lo12:STRLIT_98
    mov x1, #0
    bl print_string

    // Cargar variable global 'columna' (única: 'columna') en registro x1
    adrp x1, GV_columna
    add x1, x1, :lo12:GV_columna
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ']'
    adrp x0, STRLIT_99
    add x0, x0, :lo12:STRLIT_99
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

for_increment_167:
    // Incremento FOR
    // Evaluar expresión compleja para 'columna' (única: 'columna')
    adrp x11, GV_columna
    add x11, x11, :lo12:GV_columna
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'columna' (única: 'columna') en registro x9
    adrp x9, GV_columna
    add x9, x9, :lo12:GV_columna
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_166
for_end_168:
    // Fin FOR
for_increment_164:
    // Incremento FOR
    // Evaluar expresión compleja para 'fila' (única: 'fila')
    adrp x11, GV_fila
    add x11, x11, :lo12:GV_fila
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'fila' (única: 'fila') en registro x9
    adrp x9, GV_fila
    add x9, x9, :lo12:GV_fila
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_163
for_end_165:
    // Fin FOR
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\nProcesamiento selectivo con continue:
    // Imprimir string literal '\nProcesamiento selectivo con continue:'
    adrp x0, STRLIT_100
    add x0, x0, :lo12:STRLIT_100
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // FOR statement
    // Inicialización FOR
    // Limpiar valor residual de variable FOR 'pos'
    mov x1, #0
    adrp x2, GV_pos
    add x2, x2, :lo12:GV_pos
    str x1, [x2]
    // Declaración con primitivo: pos = 1
    adrp x1, GV_pos
    add x1, x1, :lo12:GV_pos
    mov x2, #1
    str x2, [x1]

for_loop_171:
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'pos' (única: 'pos') en registro x9
    adrp x9, GV_pos
    add x9, x9, :lo12:GV_pos
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR O IGUAL: comparar x9 <= x8
    cmp x9, x8
    cset x9, le
    cmp x9, #0
    b.eq for_end_173
    // Bloque FOR
    // Declaración con primitivo: notaPos = 0
    adrp x1, GV_notaPos
    add x1, x1, :lo12:GV_notaPos
    mov x2, #0
    str x2, [x1]

    // Declaración con primitivo: edadPos = 0
    adrp x1, GV_edadPos
    add x1, x1, :lo12:GV_edadPos
    mov x2, #0
    str x2, [x1]

    // Declaración con primitivo: nombrePos = 
    adrp x1, GV_nombrePos
    add x1, x1, :lo12:GV_nombrePos
    adrp x2, STRLIT_22
    add x2, x2, :lo12:STRLIT_22
    str x2, [x1]

    // IF statement
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'pos' (única: 'pos') en registro x9
    adrp x9, GV_pos
    add x9, x9, :lo12:GV_pos
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_174
    // Bloque IF
    // Asignar identificador 'nota1' (único: 'nota1') a variable 'notaPos' (única: 'notaPos')
    adrp x1, GV_nota1
    add x1, x1, :lo12:GV_nota1
    ldr x2, [x1]
    adrp x3, GV_notaPos
    add x3, x3, :lo12:GV_notaPos
    str x2, [x3]

    // Asignar identificador 'edad1' (único: 'edad1') a variable 'edadPos' (única: 'edadPos')
    adrp x1, GV_edad1
    add x1, x1, :lo12:GV_edad1
    ldr x2, [x1]
    adrp x3, GV_edadPos
    add x3, x3, :lo12:GV_edadPos
    str x2, [x3]

    // Asignar identificador 'estudiante1' (único: 'estudiante1') a variable 'nombrePos' (única: 'nombrePos')
    adrp x1, GV_estudiante1
    add x1, x1, :lo12:GV_estudiante1
    ldr x2, [x1]
    adrp x3, GV_nombrePos
    add x3, x3, :lo12:GV_nombrePos
    str x2, [x3]

    b endif_175
else_174:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 2 en registro x8
    mov x8, #2
    str x8, [sp, #-16]!
    // Cargar variable global 'pos' (única: 'pos') en registro x9
    adrp x9, GV_pos
    add x9, x9, :lo12:GV_pos
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_176
    // Bloque IF
    // Asignar identificador 'nota2' (único: 'nota2') a variable 'notaPos' (única: 'notaPos')
    adrp x1, GV_nota2
    add x1, x1, :lo12:GV_nota2
    ldr x2, [x1]
    adrp x3, GV_notaPos
    add x3, x3, :lo12:GV_notaPos
    str x2, [x3]

    // Asignar identificador 'edad2' (único: 'edad2') a variable 'edadPos' (única: 'edadPos')
    adrp x1, GV_edad2
    add x1, x1, :lo12:GV_edad2
    ldr x2, [x1]
    adrp x3, GV_edadPos
    add x3, x3, :lo12:GV_edadPos
    str x2, [x3]

    // Asignar identificador 'estudiante2' (único: 'estudiante2') a variable 'nombrePos' (única: 'nombrePos')
    adrp x1, GV_estudiante2
    add x1, x1, :lo12:GV_estudiante2
    ldr x2, [x1]
    adrp x3, GV_nombrePos
    add x3, x3, :lo12:GV_nombrePos
    str x2, [x3]

    b endif_177
else_176:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 3 en registro x8
    mov x8, #3
    str x8, [sp, #-16]!
    // Cargar variable global 'pos' (única: 'pos') en registro x9
    adrp x9, GV_pos
    add x9, x9, :lo12:GV_pos
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_178
    // Bloque IF
    // Asignar identificador 'nota3' (único: 'nota3') a variable 'notaPos' (única: 'notaPos')
    adrp x1, GV_nota3
    add x1, x1, :lo12:GV_nota3
    ldr x2, [x1]
    adrp x3, GV_notaPos
    add x3, x3, :lo12:GV_notaPos
    str x2, [x3]

    // Asignar identificador 'edad3' (único: 'edad3') a variable 'edadPos' (única: 'edadPos')
    adrp x1, GV_edad3
    add x1, x1, :lo12:GV_edad3
    ldr x2, [x1]
    adrp x3, GV_edadPos
    add x3, x3, :lo12:GV_edadPos
    str x2, [x3]

    // Asignar identificador 'estudiante3' (único: 'estudiante3') a variable 'nombrePos' (única: 'nombrePos')
    adrp x1, GV_estudiante3
    add x1, x1, :lo12:GV_estudiante3
    ldr x2, [x1]
    adrp x3, GV_nombrePos
    add x3, x3, :lo12:GV_nombrePos
    str x2, [x3]

    b endif_179
else_178:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 4 en registro x8
    mov x8, #4
    str x8, [sp, #-16]!
    // Cargar variable global 'pos' (única: 'pos') en registro x9
    adrp x9, GV_pos
    add x9, x9, :lo12:GV_pos
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq else_180
    // Bloque IF
    // Asignar identificador 'nota4' (único: 'nota4') a variable 'notaPos' (única: 'notaPos')
    adrp x1, GV_nota4
    add x1, x1, :lo12:GV_nota4
    ldr x2, [x1]
    adrp x3, GV_notaPos
    add x3, x3, :lo12:GV_notaPos
    str x2, [x3]

    // Asignar identificador 'edad4' (único: 'edad4') a variable 'edadPos' (única: 'edadPos')
    adrp x1, GV_edad4
    add x1, x1, :lo12:GV_edad4
    ldr x2, [x1]
    adrp x3, GV_edadPos
    add x3, x3, :lo12:GV_edadPos
    str x2, [x3]

    // Asignar identificador 'estudiante4' (único: 'estudiante4') a variable 'nombrePos' (única: 'nombrePos')
    adrp x1, GV_estudiante4
    add x1, x1, :lo12:GV_estudiante4
    ldr x2, [x1]
    adrp x3, GV_nombrePos
    add x3, x3, :lo12:GV_nombrePos
    str x2, [x3]

    b endif_181
else_180:
    // Bloque ELSE
    // IF statement
    // Cargar entero literal 5 en registro x8
    mov x8, #5
    str x8, [sp, #-16]!
    // Cargar variable global 'pos' (única: 'pos') en registro x9
    adrp x9, GV_pos
    add x9, x9, :lo12:GV_pos
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación IGUALDAD: comparar x9 == x8
    cmp x9, x8
    cset x9, eq
    cmp x9, #0
    b.eq endif_183
    // Bloque IF
    // Asignar identificador 'nota5' (único: 'nota5') a variable 'notaPos' (única: 'notaPos')
    adrp x1, GV_nota5
    add x1, x1, :lo12:GV_nota5
    ldr x2, [x1]
    adrp x3, GV_notaPos
    add x3, x3, :lo12:GV_notaPos
    str x2, [x3]

    // Asignar identificador 'edad5' (único: 'edad5') a variable 'edadPos' (única: 'edadPos')
    adrp x1, GV_edad5
    add x1, x1, :lo12:GV_edad5
    ldr x2, [x1]
    adrp x3, GV_edadPos
    add x3, x3, :lo12:GV_edadPos
    str x2, [x3]

    // Asignar identificador 'estudiante5' (único: 'estudiante5') a variable 'nombrePos' (única: 'nombrePos')
    adrp x1, GV_estudiante5
    add x1, x1, :lo12:GV_estudiante5
    ldr x2, [x1]
    adrp x3, GV_nombrePos
    add x3, x3, :lo12:GV_nombrePos
    str x2, [x3]

endif_183:
    // Fin IF
endif_181:
    // Fin IF
endif_179:
    // Fin IF
endif_177:
    // Fin IF
endif_175:
    // Fin IF
    // IF statement
    // Cargar entero literal 21 en registro x8
    mov x8, #21
    str x8, [sp, #-16]!
    // Cargar variable global 'edadPos' (única: 'edadPos') en registro x9
    adrp x9, GV_edadPos
    add x9, x9, :lo12:GV_edadPos
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR QUE: comparar x9 > x8
    cmp x9, x8
    cset x8, gt
    str x8, [sp, #-16]!
    // Cargar entero literal 80 en registro x8
    mov x8, #80
    str x8, [sp, #-16]!
    // Cargar variable global 'notaPos' (única: 'notaPos') en registro x9
    adrp x9, GV_notaPos
    add x9, x9, :lo12:GV_notaPos
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MENOR QUE: comparar x9 < x8
    cmp x9, x8
    cset x9, lt
    ldr x8, [sp], #16
    // Operación OR LÓGICO: x9 || x8
    cmp x9, #0
    csel x9, x8, x9, eq
    cmp x9, #0
    cset x9, ne
    cmp x9, #0
    b.eq endif_185
    // Bloque IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor=Saltando 
# # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # #   id nombre=nombrePos
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor= (no cumple criterios)
    // Imprimir string literal 'Saltando '
    adrp x0, STRLIT_89
    add x0, x0, :lo12:STRLIT_89
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombrePos' (única: 'nombrePos')
    adrp x0, GV_nombrePos
    add x0, x0, :lo12:GV_nombrePos
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' (no cumple criterios)'
    adrp x0, STRLIT_101
    add x0, x0, :lo12:STRLIT_101
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // CONTINUE statement - saltar al inicio del bucle
    b for_increment_172
endif_185:
    // Fin IF
    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # #   expresion tipo=0
# # # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # # #   expresion tipo=0
# # # # # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # # # # #   expresion tipo=0
# # # # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # # # #   prim tipo=9 valor=Procesando 
# # # # # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # # # # #   id nombre=nombrePos
# # # # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # # # #   prim tipo=9 valor= (cumple criterios: nota=
# # # # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor=, edad=
# # # AST node: UNKNOWN iptr=0x619b37b10263 children=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=)
    // Imprimir string literal 'Procesando '
    adrp x0, STRLIT_91
    add x0, x0, :lo12:STRLIT_91
    mov x1, #0
    bl print_string

    // Imprimir variable global 'nombrePos' (única: 'nombrePos')
    adrp x0, GV_nombrePos
    add x0, x0, :lo12:GV_nombrePos
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' (cumple criterios: nota='
    adrp x0, STRLIT_102
    add x0, x0, :lo12:STRLIT_102
    mov x1, #0
    bl print_string

    // Cargar variable global 'notaPos' (única: 'notaPos') en registro x1
    adrp x1, GV_notaPos
    add x1, x1, :lo12:GV_notaPos
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ', edad='
    adrp x0, STRLIT_103
    add x0, x0, :lo12:STRLIT_103
    mov x1, #0
    bl print_string

    // Cargar variable global 'edadPos' (única: 'edadPos') en registro x1
    adrp x1, GV_edadPos
    add x1, x1, :lo12:GV_edadPos
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #0
    bl print_string
    mov x0, x9
    bl free

    // Imprimir string literal ')'
    adrp x0, STRLIT_35
    add x0, x0, :lo12:STRLIT_35
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

for_increment_172:
    // Incremento FOR
    // Evaluar expresión compleja para 'pos' (única: 'pos')
    adrp x11, GV_pos
    add x11, x11, :lo12:GV_pos
    // Cargar entero literal 1 en registro x8
    mov x8, #1
    str x8, [sp, #-16]!
    // Cargar variable global 'pos' (única: 'pos') en registro x9
    adrp x9, GV_pos
    add x9, x9, :lo12:GV_pos
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    b for_loop_171
for_end_173:
    // Fin FOR
    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\n--- RESUMEN FINAL ---
    // Imprimir string literal '\n--- RESUMEN FINAL ---'
    adrp x0, STRLIT_104
    add x0, x0, :lo12:STRLIT_104
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    // Declaración con casting automático: notaMaxima = nota1 (char->int)
    adrp x1, GV_notaMaxima
    add x1, x1, :lo12:GV_notaMaxima
    adrp x2, GV_nota1
    add x2, x2, :lo12:GV_nota1
    ldrb w2, [x2]
    sxtb x2, w2
    str x2, [x1]

    // IF statement
    // Cargar variable global 'notaMaxima' (única: 'notaMaxima') en registro x8
    adrp x8, GV_notaMaxima
    add x8, x8, :lo12:GV_notaMaxima
    ldr x8, [x8]
    str x8, [sp, #-16]!
    // Cargar variable global 'nota2' (única: 'nota2') en registro x9
    adrp x9, GV_nota2
    add x9, x9, :lo12:GV_nota2
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR QUE: comparar x9 > x8
    cmp x9, x8
    cset x9, gt
    cmp x9, #0
    b.eq endif_187
    // Bloque IF
    // Asignar identificador 'nota2' (único: 'nota2') a variable 'notaMaxima' (única: 'notaMaxima')
    adrp x1, GV_nota2
    add x1, x1, :lo12:GV_nota2
    ldr x2, [x1]
    adrp x3, GV_notaMaxima
    add x3, x3, :lo12:GV_notaMaxima
    str x2, [x3]

    // Asignar identificador 'estudiante2' (único: 'estudiante2') a variable 'estudianteMejor' (única: 'estudianteMejor')
    adrp x1, GV_estudiante2
    add x1, x1, :lo12:GV_estudiante2
    ldr x2, [x1]
    adrp x3, GV_estudianteMejor
    add x3, x3, :lo12:GV_estudianteMejor
    str x2, [x3]

endif_187:
    // Fin IF
    // IF statement
    // Cargar variable global 'notaMaxima' (única: 'notaMaxima') en registro x8
    adrp x8, GV_notaMaxima
    add x8, x8, :lo12:GV_notaMaxima
    ldr x8, [x8]
    str x8, [sp, #-16]!
    // Cargar variable global 'nota3' (única: 'nota3') en registro x9
    adrp x9, GV_nota3
    add x9, x9, :lo12:GV_nota3
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR QUE: comparar x9 > x8
    cmp x9, x8
    cset x9, gt
    cmp x9, #0
    b.eq endif_189
    // Bloque IF
    // Asignar identificador 'nota3' (único: 'nota3') a variable 'notaMaxima' (única: 'notaMaxima')
    adrp x1, GV_nota3
    add x1, x1, :lo12:GV_nota3
    ldr x2, [x1]
    adrp x3, GV_notaMaxima
    add x3, x3, :lo12:GV_notaMaxima
    str x2, [x3]

    // Asignar identificador 'estudiante3' (único: 'estudiante3') a variable 'estudianteMejor' (única: 'estudianteMejor')
    adrp x1, GV_estudiante3
    add x1, x1, :lo12:GV_estudiante3
    ldr x2, [x1]
    adrp x3, GV_estudianteMejor
    add x3, x3, :lo12:GV_estudianteMejor
    str x2, [x3]

endif_189:
    // Fin IF
    // IF statement
    // Cargar variable global 'notaMaxima' (única: 'notaMaxima') en registro x8
    adrp x8, GV_notaMaxima
    add x8, x8, :lo12:GV_notaMaxima
    ldr x8, [x8]
    str x8, [sp, #-16]!
    // Cargar variable global 'nota4' (única: 'nota4') en registro x9
    adrp x9, GV_nota4
    add x9, x9, :lo12:GV_nota4
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR QUE: comparar x9 > x8
    cmp x9, x8
    cset x9, gt
    cmp x9, #0
    b.eq endif_191
    // Bloque IF
    // Asignar identificador 'nota4' (único: 'nota4') a variable 'notaMaxima' (única: 'notaMaxima')
    adrp x1, GV_nota4
    add x1, x1, :lo12:GV_nota4
    ldr x2, [x1]
    adrp x3, GV_notaMaxima
    add x3, x3, :lo12:GV_notaMaxima
    str x2, [x3]

    // Asignar identificador 'estudiante4' (único: 'estudiante4') a variable 'estudianteMejor' (única: 'estudianteMejor')
    adrp x1, GV_estudiante4
    add x1, x1, :lo12:GV_estudiante4
    ldr x2, [x1]
    adrp x3, GV_estudianteMejor
    add x3, x3, :lo12:GV_estudianteMejor
    str x2, [x3]

endif_191:
    // Fin IF
    // IF statement
    // Cargar variable global 'notaMaxima' (única: 'notaMaxima') en registro x8
    adrp x8, GV_notaMaxima
    add x8, x8, :lo12:GV_notaMaxima
    ldr x8, [x8]
    str x8, [sp, #-16]!
    // Cargar variable global 'nota5' (única: 'nota5') en registro x9
    adrp x9, GV_nota5
    add x9, x9, :lo12:GV_nota5
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación MAYOR QUE: comparar x9 > x8
    cmp x9, x8
    cset x9, gt
    cmp x9, #0
    b.eq endif_193
    // Bloque IF
    // Asignar identificador 'nota5' (único: 'nota5') a variable 'notaMaxima' (única: 'notaMaxima')
    adrp x1, GV_nota5
    add x1, x1, :lo12:GV_nota5
    ldr x2, [x1]
    adrp x3, GV_notaMaxima
    add x3, x3, :lo12:GV_notaMaxima
    str x2, [x3]

    // Asignar identificador 'estudiante5' (único: 'estudiante5') a variable 'estudianteMejor' (única: 'estudianteMejor')
    adrp x1, GV_estudiante5
    add x1, x1, :lo12:GV_estudiante5
    ldr x2, [x1]
    adrp x3, GV_estudianteMejor
    add x3, x3, :lo12:GV_estudianteMejor
    str x2, [x3]

endif_193:
    // Fin IF
    // Declaración con expresión compleja: sumaTotal = expresión
    adrp x11, GV_sumaTotal
    add x11, x11, :lo12:GV_sumaTotal
    // DEBUG: Variable 'sumaTotal' tipo=5
    // Cargar variable global 'nota5' (única: 'nota5') en registro x8
    adrp x8, GV_nota5
    add x8, x8, :lo12:GV_nota5
    ldr x8, [x8]
    str x8, [sp, #-16]!
    // Cargar variable global 'nota4' (única: 'nota4') en registro x8
    adrp x8, GV_nota4
    add x8, x8, :lo12:GV_nota4
    ldr x8, [x8]
    str x8, [sp, #-16]!
    // Cargar variable global 'nota3' (única: 'nota3') en registro x8
    adrp x8, GV_nota3
    add x8, x8, :lo12:GV_nota3
    ldr x8, [x8]
    str x8, [sp, #-16]!
    // Cargar variable global 'nota2' (única: 'nota2') en registro x8
    adrp x8, GV_nota2
    add x8, x8, :lo12:GV_nota2
    ldr x8, [x8]
    str x8, [sp, #-16]!
    // Cargar variable global 'nota1' (única: 'nota1') en registro x9
    adrp x9, GV_nota1
    add x9, x9, :lo12:GV_nota1
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x9, x9, x8
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x9, x9, x8
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x9, x9, x8
    ldr x8, [sp], #16
    // Operación SUMA de enteros
    add x2, x9, x8
    str x2, [x11]

    // Declaración con expresión compleja: promedioFinal = expresión
    adrp x11, GV_promedioFinal
    add x11, x11, :lo12:GV_promedioFinal
    // DEBUG: Variable 'promedioFinal' tipo=8
    // Cargar número flotante literal 5.0 en registro d8
    adrp x8, NUMLIT_1
    ldr d8, [x8, :lo12:NUMLIT_1]
    fmov x8, d8
    str x8, [sp, #-16]!
    // Cargar variable global 'sumaTotal' (única: 'sumaTotal') en registro x9
    adrp x9, GV_sumaTotal
    add x9, x9, :lo12:GV_sumaTotal
    ldr x9, [x9]
    ldr x8, [sp], #16
    // Operación DIVISIÓN de double
    // Evaluar operandos directamente para división
    // Cargar variable global 'sumaTotal' (única: 'sumaTotal') en registro x9
    adrp x9, GV_sumaTotal
    add x9, x9, :lo12:GV_sumaTotal
    ldr x9, [x9]
    // Convertir entero x9 a double d9
    scvtf d9, x9
    // Cargar literal double 5.0 directamente en d8
    adrp x8, NUMLIT_1
    ldr d8, [x8, :lo12:NUMLIT_1]
    fdiv d2, d9, d8
    // Mantener resultado en d2 para variables double, convertir a x2 para variables enteras
    fcvtns x2, d2
    str d2, [x11]

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# #   expresion tipo=0
# # # AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
# # #   expresion tipo=0
# # # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # # #   prim tipo=9 valor=Mejor estudiante: 
# # # # AST node: IDENTIFICADOR iptr=0x619b37b152c8 children=0
# # # #   id nombre=estudianteMejor
# # # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# # #   prim tipo=9 valor= con 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Mejor estudiante: '
    adrp x0, STRLIT_105
    add x0, x0, :lo12:STRLIT_105
    mov x1, #0
    bl print_string

    // Imprimir variable global 'estudianteMejor' (única: 'estudianteMejor')
    adrp x0, GV_estudianteMejor
    add x0, x0, :lo12:GV_estudianteMejor
    // tipo tag=9
    mov x1, #9
    mov x2, #0
    bl print_any_gv

    // Imprimir string literal ' con '
    adrp x0, STRLIT_26
    add x0, x0, :lo12:STRLIT_26
    mov x1, #0
    bl print_string

    // Cargar variable global 'notaMaxima' (única: 'notaMaxima') en registro x1
    adrp x1, GV_notaMaxima
    add x1, x1, :lo12:GV_notaMaxima
    ldr x1, [x1]
    mov w0, #1
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: EXPRESION_LENGUAJE iptr=0x619b37b14c32 children=2
#   expresion tipo=0
# # AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
# #   prim tipo=9 valor=Promedio general: 
# # AST node: UNKNOWN iptr=0x619b37b10263 children=0
    // Imprimir string literal 'Promedio general: '
    adrp x0, STRLIT_106
    add x0, x0, :lo12:STRLIT_106
    mov x1, #0
    bl print_string

    // Cargar variable double 'promedioFinal' en x1 como bits
    adrp x1, GV_promedioFinal
    ldr x1, [x1, :lo12:GV_promedioFinal]
    mov w0, #2
    bl string_value_of_any
    mov x9, x0
    mov x0, x9
    mov x1, #1
    bl print_string
    mov x0, x9
    bl free

    // ===== Fin System.out.println =====

    // ===== System.out.println =====
# AST node: PRIMITIVO iptr=0x619b37b153c5 children=0
#   prim tipo=9 valor=\n=== PROCESAMIENTO COMPLETADO ===
    // Imprimir string literal '\n=== PROCESAMIENTO COMPLETADO ==='
    adrp x0, STRLIT_107
    add x0, x0, :lo12:STRLIT_107
    mov x1, #1
    bl print_string

    // ===== Fin System.out.println =====

    .section .data
    .align 8
NUMLIT_1:
    .quad 0x4014000000000000

    .section .text
    mov x0, #0
    mov x8, #93
    svc #0
